% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{cdisc_eff_n}
\alias{cdisc_eff_n}
\title{Efficacy-population BigN per arm}
\format{
A data frame with 4 rows and 3 columns; same schema as
\link{cdisc_saf_n} (\code{arm}, \code{arm_short}, \code{n}).
}
\source{
Derived in \code{data-raw/bundle-demo.R} from the per-arm BOR
denominator computed inside the \code{cdisc_eff_resp} pipeline.
}
\usage{
cdisc_eff_n
}
\description{
Per-arm subject counts (BigN) for the efficacy population used by
\code{cdisc_eff_resp} / \code{eff_resp_card} — subjects with a \code{BOR} record in
\code{pharmaverseadam::adrs_onco}. Same two-column naming convention
as \code{cdisc_saf_n}; the totals differ from \code{cdisc_saf_n} because not every
safety-pop subject contributes a best-overall-response record.
}
\examples{
# Efficacy BigN joined into column headers.
ne <- stats::setNames(cdisc_eff_n$n, cdisc_eff_n$arm_short)
col_spec(label = "Placebo\nN={ne['placebo']}")@label
}
\seealso{
\link{cdisc_saf_n} for the safety-population counterpart.
}
\keyword{datasets}
