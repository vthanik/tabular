% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend_typst_pdf.R
\name{check_typst}
\alias{check_typst}
\title{Check Typst availability for PDF output}
\usage{
check_typst(quiet = FALSE)
}
\arguments{
\item{quiet}{\emph{Suppress the printed cli report.}
\verb{<logical(1)>: default FALSE}. When \code{TRUE}, runs the checks and
returns the result invisibly without printing. Use in scripts
that branch on the return value.}
}
\value{
\emph{Invisibly returns a data frame} with one row per family
in the resolved default font chain and columns \code{font}
(\verb{<character>}) and \code{available} (\verb{<logical>}, \code{NA} when the font
list could not be read), plus attributes \code{typst_version}
(\verb{<numeric_version | NA>}) and \code{typst_command} (\verb{<character(1) | NA_character_>}, the discovered compiler invocation). Side
effect: prints a cli report with a status marker per check and,
when anything is missing, the remedy.
}
\description{
Reports whether a typst compiler is available (the standalone
\code{typst} binary, or the copy bundled inside Quarto), whether its
version meets tabular's floor, and which families of the default
font chain the compiler can actually see. Run this before
\code{emit(spec, "out.pdf", format = "typst")} on a fresh machine, and
whenever a Typst-compiled PDF renders in an unexpected face.
}
\details{
\strong{Binary discovery.} A standalone \code{typst} on the \code{PATH} wins;
otherwise the check falls back to \verb{quarto typst} (Quarto >= 1.4
bundles the typst compiler — so most machines with RStudio / Posit
Workbench already have one). The Typst PDF path needs no TeX
installation and no package downloads at all.

\strong{Fonts are the silent failure mode.} Where a missing LaTeX
package stops a compile with an error, typst substitutes a missing
font family silently and only notes it on the compiler's stderr.
The check therefore lists every family in the resolved default
chain with its availability. The chain is a \emph{fallback} chain:
typst renders in the first family it can see, so a missing later
member is normal cross-OS variance, not a defect — the check
reports ready as soon as any family resolves, and names the face
PDFs will render in. \code{emit()} additionally warns after a Typst
compile when a family the user explicitly named cannot be found
(missing members of the built-in fallback chains stay silent,
matching the other backends' silent font-substitution behaviour).

\strong{Status markers:}
\itemize{
\item \code{v} — found (binary; version at or above the floor; font family
visible to typst).
\item \code{x} — missing (no binary; version below the floor; font family
not visible).
\item \verb{?} — could not be determined (e.g. the font list could not be
read); treated as missing for remediation.
}
}
\examples{
# ---- Example 1: Audit the Typst toolchain before emitting ----
#
# Run check_typst() on a fresh machine to confirm a typst compiler
# is present and to see which families of the default font chain it
# resolves. Where no binary is found every row reports `?` and the
# remedy lines print; the call never errors.
check_typst()

}
\seealso{
\strong{Companion diagnostics:} \code{\link[=check_latex]{check_latex()}}, \code{\link[=check_fonts]{check_fonts()}}.

\strong{Consumes the result:} \code{\link[=emit]{emit()}}.
}
