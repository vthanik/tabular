% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{print.tabular_spec}
\alias{print.tabular_spec}
\title{Print a \code{tabular_spec}}
\arguments{
\item{x}{A \code{tabular_spec} object.}

\item{...}{Ignored.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Pretty-prints a \code{tabular_spec} as a structured cli tree showing
data dimensions, titles, footnotes, pagination, and any column /
row / span / style configuration the spec carries. Useful during
pipeline construction to inspect the spec before rendering.
}
\details{
Phase 1b upgrade: once the HTML backend lands, this method will
render the spec to a tempfile via \code{\link[=tb_render]{tb_render()}} and open it in
the active viewer (\code{getOption("viewer")}) when called
interactively — mirroring the \code{gt::gt()} and \code{tinytable::tt()}
UX. Quarto / Rmd / pkgdown inline rendering arrives at the same
time via a \code{knit_print.tabular_spec} method. Until then, the cli
tree view is the print output.
}
\examples{
tb_table(saf_demo, titles = c("Table 14.1.1", "Demographics"))

}
