% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preset.R
\name{preset}
\alias{preset}
\title{Override the render preset on a spec}
\usage{
preset(.spec, ..., .template = NULL, .style = NULL, .reset = FALSE)
}
\arguments{
\item{.spec}{\emph{The spec to attach the preset to.}
\verb{<tabular_spec | figure_spec>: required}. Dot-prefixed so R's
partial argument matching cannot accidentally bind a knob name in
\code{...} to the spec slot.

\strong{Note:} a \code{\link[=figure]{figure()}} spec accepts the page-geometry knobs
(\code{paper_size}, \code{orientation}, \code{margins}, \code{font_size}, \code{font_family},
\code{pagehead}, \code{pagefoot}, ...) plus the cosmetic surface knobs
(\code{alignment} / \code{fonts} / \code{colors} / \code{padding}) that target its chrome
surfaces, the titles and footnotes, e.g.
\code{fonts = list(titles = c(size = 14))}. A cosmetic knob that targets a
table-only surface (\code{body} / \code{header} / \code{subgroup}), a \code{rules} knob
(the rules sit on the header band a figure lacks), and the
\code{.template} / \code{.style} style templates are rejected, since a figure
has no such surfaces.}

\item{...}{\emph{Named preset knobs.} Any subset of the preset knobs the
\code{preset_spec} class carries. Knob values are validated against
the class's enum / length / type rules; bad values raise
\code{tabular_error_input}. Unknown knob names raise
\code{tabular_error_input} with the recognised set listed.

Recognised knobs:
\itemize{
\item \strong{\code{font_size}} — body point size. \verb{<numeric(1)>}.
\item \strong{\code{font_family}} — body font family. \verb{<character | character(1)>}.
Default \code{"mono"}. Three accepted shapes:
\enumerate{
\item \strong{Generic family} — \code{"mono"} (default), \code{"serif"},
\code{"sans"} (CSS aliases \code{"monospace"} / \code{"sans-serif"}
also recognised). The resolver expands to a per-backend
chain that leads with the Linux-installed
\strong{Liberation} face (Posit Workbench / Domino / Citrix
/ RStudio Server), then the Microsoft Office face
(Courier New / Times New Roman / Arial) for desktop
Win / Mac consumers, then TeX Gyre for LaTeX compile,
then the CSS generic for HTML. Liberation Mono / Serif
/ Sans are metric-compatible with Courier New / TNR /
Arial, so layout, line breaks, and decimal alignment
hold across every render context. The mono default
matches the dominant submission-TFL convention where
deterministic glyph widths drive \verb{n (\%)} cell alignment.
\item \strong{Named alias} — \code{"Times"}, \code{"Times New Roman"},
\code{"Arial"}, \code{"Helvetica"}, \code{"Courier"}, \code{"Courier New"}.
These PostScript-era names alias to the appropriate
generic family (Times -> serif, Arial / Helvetica ->
sans, Courier -> mono) and emit the same expanded chain.
Honours the user's intent ("I want Times-like
rendering") on every OS instead of hard-erroring on
a Linux server with no TNR installed.
\item \strong{Named font} — \code{"Inter"}, \code{"JetBrains Mono"},
\code{"Source Serif Pro"}, sponsor-specific face, etc.
Emitted verbatim with no fallback fabricated. The
consuming app (browser, xelatex, Word, LibreOffice)
resolves the name against its own font matcher. RTF
and DOCX fall back to the consuming app's substitution
table when the name is missing; xelatex hard-errors at
compile time; HTML browsers fall through to the
browser's default font (not necessarily class-matched).
\item \strong{Explicit stack} — \code{c("Inter", "Helvetica", "sans")}.
User owns the chain. Returned verbatim — alias lookup
is \strong{bypassed}, so \code{c("Times", "Times")} honours the
exact name with no chain expansion (escape hatch for
users who genuinely want exact-name semantics).
}

\strong{Note:} Adobe Source Pro is no longer the default lead.
Source Pro is not pre-installed on production Linux
servers, so leading with it walks through 2-3 missing
names before resolving. Users who installed Source Pro
can opt in via the explicit-stack form
(\code{c("Source Serif Pro", "serif")}).

\strong{What you see in Word's font dropdown vs. what renders.}
When you open a tabular-generated \code{.rtf} in Word on
macOS or Windows, the font dropdown displays the file's
\emph{requested} face — \code{"Liberation Mono"} by default
(the Linux-server-installed face). The rendered text on
screen is whatever Word's \verb{\\\\*\\\\falt} substitution
resolved to — typically Courier New on macOS /
Windows. This is correct: Liberation Mono and Courier
New are metric-compatible by design, so the rendered
layout (line breaks, decimal alignment, page breaks) is
identical regardless of which face Word actually used to
render. The same \verb{\\\\*\\\\falt} substitution model applies
to serif (Liberation Serif -> Times New Roman) and sans
(Liberation Sans -> Arial).

\strong{How to force Office names as the primary.} If
reviewers will be confused by seeing \code{"Liberation Mono"}
in the Word font dropdown (cosmetic concern; doesn't
affect rendering), pass an explicit length>1 stack with
the Office name first. The resolver returns the vector
verbatim — no alias lookup, no chain expansion — so the
RTF file then names the Office face as primary and your
chosen alternate as \verb{\\\\*\\\\falt}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{preset(font_family = c("Courier New", "Courier", "Liberation Mono"))
}\if{html}{\out{</div>}}

This is the canonical escape hatch for authors who know
their consumer audience is Mac / Windows Word users and
want the dropdown to show the Office face directly.
\item \strong{\code{orientation}} — page orientation.
\verb{<character(1)>}. One of \code{"landscape"} (default),
\code{"portrait"}.
\item \strong{\code{paper_size}} — paper key.
\verb{<character(1)>}. One of \code{"letter"} (default), \code{"a4"}.
\item \strong{\code{margins}} — page margins in inches.
\verb{<numeric(1) | numeric(4)>}. Length 1 = all four sides;
length 4 = top, right, bottom, left.
\item \strong{\code{pagehead}}, \strong{\code{pagefoot}} — per-page header / footer
band content. \verb{<list>}. Each band is a named list with
slots from \code{left} / \code{center} / \code{right}; every other slot
name is rejected. Each slot accepts \code{NULL} (omit), a
character scalar, a character vector (multi-row content),
or an \code{\link{inline_ast}}. Empty \code{list()} (the default) -> no
band emitted.

\strong{Single-row form} (scalar slots):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{pagehead = list(
  left   = "Protocol: ABC-123",
  center = "Draft",
  right  = "Page \{page\} of \{npages\}"
)
}\if{html}{\out{</div>}}

\strong{Multi-row form} (vector slots, index-aligned):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{pagehead = list(
  left  = c("Protocol: ABC-123", "Analysis Set: Safety"),
  right = "Page \{page\} of \{npages\}"   # scalar -> body-edge row
)
}\if{html}{\out{</div>}}

\strong{Growth direction.} Vector index 1 = body edge; index N
= far from body. \code{pagehead} rows stack \strong{upward} away
from the table (the row closest to the table is index 1).
\code{pagefoot} rows stack \strong{downward} away from the table
(the row closest to the table is index 1). Shorter slots
pad with \code{""} at the FAR end (high index), so a scalar
slot naturally lands on the body-edge row.

\strong{Token vocabulary} — substituted into slot text:\tabular{lll}{
   Token \tab Phase \tab Expansion \cr
   \code{{page}} \tab backend \tab current page number (field code) \cr
   \code{{npages}} \tab backend \tab total page count (field code) \cr
   \code{{program}} \tab engine \tab calling script's base name \cr
   \code{{program_path}} \tab engine \tab calling script's full path \cr
   \code{{datetime}} \tab engine \tab \verb{DDMMMYYYY HH:MM:SS} UTC (render time) \cr
}


\code{{program}}, \code{{program_path}}, and \code{{datetime}} resolve
once per render (at \code{\link[=as_grid]{as_grid()}} / \code{\link[=emit]{emit()}}); \code{{page}}
and \code{{npages}} resolve per page (filled in by Word /
xelatex / the browser's print engine at view time). The
program tokens walk a 5-mode detection chain — RStudio
API, \code{source()} frame, Rscript / R CMD BATCH commandArgs
(covers Domino + Linux batch + CI), knitr current_input,
fallback \code{"<interactive>"}.
\item \strong{\code{rules}} — the single border vocabulary (replaces the old
\code{borders} knob). String sugar \code{"booktabs"} (default, the
clinical baseline), \code{"grid"}, \code{"frame"}, \code{"none"}; a single
\code{\link[=brdr]{brdr()}} broadcast to every active rule; or a named list
keyed by the nine rule names (\code{toprule}, \code{midrule},
\code{bottomrule}, \code{spanrule}, \code{rowrule}, \code{footnoterule},
\code{leftrule}, \code{rightrule}, \code{colrule}) — unlisted rules keep
their default, and the bare string \code{"none"} drops one.
\code{rules = list(rowrule = brdr())} reproduces the old
\code{hlines = "all"}.

\strong{\code{bottomrule} vs \code{footnoterule}.} These are mutually
exclusive: exactly one rule sits at the data -> footnote
boundary. The default is \code{bottomrule} (the table's bottom
edge); \code{footnoterule} (a table-width rule opening the
footnote section) is OFF by default. As a distinct
footnote-section rule, \code{footnoterule} is drawn only by the
paginated backends — \strong{RTF, LaTeX / PDF, and DOCX}. The
\strong{HTML} backend is continuous (non-paginated) and has no
separate footnote section, so it folds both into one rule:
whichever of \code{bottomrule} / \code{footnoterule} is active becomes
the table's bottom edge (\code{bottomrule} wins when both are
set). Setting \code{footnoterule} therefore still draws a closing
rule on HTML — rendered as the \code{bottomrule}.
\item \strong{\code{spacing}} — region-keyed blank-line control. A named list
keyed by \code{title} / \code{body} / \code{subgroup} / \code{footnote}, each a
named numeric \code{c(above = , below = )} (footnote: \code{above}
only). Default is the one blank line above and
below the title block. Two adjoining region-sides that target
the same physical gap resolve to the MAX (never the sum), so a
gap is never accidentally doubled.
\item \strong{\code{stripe}} — zebra body-row fills. A single colour (applied
to even rows) or a named \code{c(odd = , even = )}; \code{NULL}
(default) is off.
\item \strong{\code{indent_size}} — row-label indent width, in monospace-
space units. \verb{<integer(1)>}. Default \code{2L}. Each indent level
adds this many space-widths of left padding to the cell.
\code{0L} disables the indent prefix entirely. Backends with
native padding-left semantics (HTML / LaTeX / RTF / DOCX /
PDF) emit this as cell padding so wrapped continuation lines
align with the indented baseline; Markdown carries the literal
space-prefix.
Block alignment for the title / footnote / header / subgroup /
body surfaces is set via the \code{alignment} named-list knob
(\code{alignment = list(title_halign = "left", ...)}), not a scalar
knob; blank-line spacing is set via \code{spacing} (above).
\item \strong{\code{na_text}} — global NA fallback. \verb{<character(1)>}.
\item \strong{\code{decimal_metrics}} — decimal-padding metric.
\verb{<character(1)>}. \code{"afm"} (default) measures glyphs with the
bundled Core font metrics, so decimal columns align
width-exact in proportional fonts (to within one padding
space of rounding; exact in Courier). \code{"chars"} pads by
character count — exact in monospaced faces only. Markdown
output always pads by character count, the correct geometry
for a text medium.
\item \strong{\code{decimal_markers}} — missing-value tokens recognised by
\code{col_spec(align = "decimal")}. \verb{<character>}. Default
\code{c("NR", "NE", "NC", "ND", "BLQ")}. A cell whose trimmed
value is one of these is treated as a non-numeric \emph{marker}:
it is shown and right-aligned in the column rather than
parsed as a number, and a marker appearing inside a
compound (e.g. the upper bound of \code{14.3 (11.2, NR)}) is
preserved and slot-aligned. Excludes \code{"-"}, \code{"NA"}, and
\code{"INF"}/\code{"-INF"} by default: \code{"-"} collides with range
separators, \code{"NA"} is handled by \code{na_text}, and infinities
are real values. Set to \code{character(0)} to disable marker
handling.
\item \strong{\code{width_mode}} — table-level column-sizing policy. Mirrors
Word's Table Layout menu. \verb{<character(1)>}. One of:
\itemize{
\item \strong{\code{"content"}} \emph{(default)} — Each column auto-sized to
\code{max(body, header)}. The table doesn't fill the page.
Word's "Auto-fit Contents".
\item \strong{\code{"window"}} — Auto-sized columns expand to share the
residual page width equally. Pinned and percent columns
keep their pins. Word's "Auto-fit Window".
\item \strong{\code{"fixed"}} — Only explicit per-column widths drive
the layout. Auto-sized columns collapse to a minimum
sliver. Word's "Fixed Column Width".
}

\strong{Interaction:} Pair with \code{col_spec(width = ...)} pins to
drive the layout under \code{"window"} / \code{"fixed"}. Under
\code{"content"}, pins still take priority over auto columns.

\strong{HTML backend.} \code{width_mode} drives paper backends
(LaTeX / RTF / PDF / DOCX) only. HTML is unconditionally
responsive — the table always fills its parent and
columns wrap when the viewport narrows, regardless of
\code{width_mode}. Per-column widths (\code{col_spec(width)}) emit
verbatim into the HTML colgroup per the gt convention.
\item \strong{\code{empty_text}} — house-style \emph{wording} for the empty-state
message shown when a spec resolves to zero data rows.
\verb{<character(1)>}. The resolution is spec arg -> preset knob ->
built-in default: a per-table \code{tabular(empty_text = ...)} wins,
else this preset knob (set once via \code{\link[=set_preset]{set_preset()}} for a whole
house style), else the built-in \code{"No data available to report"}.
Glue \code{{}} and \code{\link[=md]{md()}} / \code{\link[=html]{html()}} inline formatting are
honoured, exactly like a title line.
\item \strong{\code{empty_halign}} / \strong{\code{empty_valign}} — placement of the
empty-state message within the body content-box when a spec
resolves to zero data rows. The message \emph{wording} comes from
\code{empty_text} (above); these two knobs are the cosmetic
\emph{placement}, so they ride the preset and cascade with the house
style. \verb{<character(1)>} each, defaulting to centre x middle:
\itemize{
\item \strong{\code{empty_halign}} — \code{"left"}, \code{"center"} \emph{(default)}, or
\code{"right"}. Horizontal anchor of the message line.
\item \strong{\code{empty_valign}} — \code{"top"}, \code{"middle"} \emph{(default)}, or
\code{"bottom"}. Vertical anchor of the message within the page
body, between the top and bottom margins.
}

\strong{Interaction:} valign is exact on the paged backends
(RTF / PDF / DOCX): the table chrome (titles, subgroup banner,
column-header band) relocates into the page margins, and the
message is centred by the backend's native section vertical
alignment. HTML renders the message in flow under the column
header and Markdown has no page geometry, so both treat valign
as a no-op.

\strong{Note:} this is the symmetric \code{halign} / \code{valign} placement
pair, shared with \code{\link[=style]{style()}} and the other \verb{*_halign} /
\verb{*_valign} alignment keys. It is deliberately distinct from
\code{col_spec(align = ...)}, whose extra \code{"decimal"} mode makes it a
column-content knob rather than a pure two-axis anchor.
\item \strong{\code{whitespace}} — how significant ASCII spaces in labels and
cells render. \verb{<character(1)>}. One of:
\itemize{
\item \strong{\code{"preserve"}} \emph{(default)} — leading, trailing, and
interior runs of 2+ spaces become the backend
non-breaking token (\verb{&nbsp;} / \code{~} / \verb{\\~}; DOCX preserves
via \code{xml:space}), so a hand-built indent like
\code{col_spec(label = "    Placebo")} renders verbatim across
every backend. A single interior space stays breakable, so
cells still wrap.
\item \strong{\code{"collapse"}} — leave the backend's native run-folding
in place (HTML / md / LaTeX collapse runs to one space).
}

\strong{Note:} never affects \code{col_spec(align = "decimal")} padding,
which uses U+00A0 and is preserved unconditionally.
\item \strong{\code{footnote_markers}} — the glyph scheme for \code{\link[=footnote]{footnote()}}
markers, which the engine allocates once in reading order.
\verb{<character(1)>}. One of:
\itemize{
\item \strong{\code{"letters"}} \emph{(default)} — \code{a}, \code{b}, …, \code{z}, \code{aa}, \code{ab},
… (bijective base-26).
\item \strong{\code{"numbers"}} — \code{1}, \code{2}, \code{3}, …
\item \strong{\code{"symbols"}} — Lamport's sequence \code{*}, \verb{†}, \verb{‡}, \verb{§},
\verb{¶}, \verb{‖}, then doubled (\verb{**}, \verb{††}, …) once it spills past
the sixth.
}

\strong{Interaction:} a note's \emph{anchor} is fixed by \code{\link[=footnote]{footnote()}};
its \emph{scheme} (this knob) and \emph{label} (\code{footnote_label}) are
resolved from the active preset at render, so flipping either
re-letters every marker at once.
\item \strong{\code{footnote_label}} — block-line template for a \code{\link[=footnote]{footnote()}}
marker. \verb{<character(1)>}. Default \code{"{m}"}; the \code{{m}} token is
replaced by the allocated marker, so \code{"[{m}]"} prints \verb{[a]}
ahead of the note text on the footnote line.
\item \strong{\code{cell_padding}} — cell padding in points, CSS shorthand of
length 1 / 2 / 4 (\code{all} | \verb{vertical horizontal} |
\verb{top right bottom left}), parsed by the same length rule as
\code{margins}. \verb{<numeric>: default c(0, 5.4)} (vertical 0,
horizontal 5.4pt). The single source of truth for both auto
column-width measurement (left + right) and every backend's
cell margin, so measured and rendered widths agree.

\strong{Interaction:} A body per-side padding override
(\code{preset(padding = list(body = ...))} or
\code{style(.at = cells_body(), padding = ...)}) takes precedence at
both measurement and render.

\strong{Note:} DOCX and LaTeX render left / right exactly; RTF
(\verb{\\\\trgaph} is one symmetric gap) renders the average, so the
total width still matches but the two sides look equal.
}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Landscape A4, 8pt body, slim margins for one wide table.
preset(
  orientation = "landscape",
  paper_size  = "a4",
  font_size   = 8,
  margins     = c(0.75, 0.5, 0.75, 0.5)
)
}\if{html}{\out{</div>}}}

\item{.template}{\emph{A \code{preset_spec} to bulk-apply before \code{...}.}
\verb{<preset_spec | NULL>: default NULL}. When supplied, every knob
the template has set away from its factory default feeds in as
the base layer; user-supplied \code{...} knobs then merge on top.
List-valued knobs (\code{rules}, \code{fonts}, \code{colors}, \code{padding},
\code{alignment}) shallow-merge per key; scalars replace. Use this
to layer a house-style \code{preset_spec} onto a chain without
restating its knobs.}

\item{.style}{\emph{A \code{style_template()} to layer onto the cascade.}
\verb{<style_template | NULL>: default NULL}. When supplied, every
layer the template has accumulated via \code{\link[=style]{style()}} is replayed
in order at engine time, after the per-spec \code{\link[=style]{style()}} layers
on \code{.spec}. Use this to attach a sponsor's reusable house style
to a chain without restating every per-region rule.}

\item{.reset}{\emph{Discard the spec's existing preset before applying
\code{...}.} \verb{<logical(1)>: default FALSE}. When \code{TRUE}, the spec's
prior \code{preset_spec} (if any) is dropped and \code{...} knobs are
merged onto fresh \code{preset_spec()} defaults. With no knobs, the
per-spec preset is cleared back to NULL (the spec falls through
to \code{\link[=set_preset]{set_preset()}} or \code{preset_spec()} defaults).}
}
\value{
\emph{The updated \code{tabular_spec}.} Continue chaining with
\code{\link[=paginate]{paginate()}}, \code{\link[=style]{style()}}, then render via \code{\link[=emit]{emit()}} (or
resolve without I/O via \code{\link[=as_grid]{as_grid()}}).
}
\description{
Attach a \code{preset_spec} to a \code{tabular_spec}, carrying page-geometry
knobs (paper, orientation, margins, body font_size + family, h-rule
policy, decimal metric, typography defaults). The engine consults
the per-spec preset first when computing the per-page row budget,
decimal-aligned column widths, and the chrome that the backend
renders around the body grid.
}
\details{
\strong{Per-spec, chained.} \code{preset()} is the per-spec override — a
verb that returns a modified spec, composable on the pipe alongside
\code{\link[=cols]{cols()}} / \code{\link[=headers]{headers()}} / \code{\link[=paginate]{paginate()}}. Use it when a single
table needs a one-off geometry (e.g. landscape A4 for one wide
efficacy summary inside a portfolio of portrait letter tables).

\strong{Merge, not replace.} A second \code{preset()} call merges its scalar
knobs onto the spec's existing preset; unspecified knobs keep
their prior value. The five named-list knobs (\code{alignment} /
\code{rules} / \code{fonts} / \code{colors} / \code{padding}) lower to \code{style_layer}
records on \code{preset@style} via \code{.preset_args_to_layers()}
(internal) and append in call order; layer order is precedence
within the engine cascade, so a later \code{preset()} call's lowered
attribute wins over an earlier one at the cell. Pass \code{.reset = TRUE}
to discard the existing knobs and start from \code{preset_spec()}
defaults. \code{preset(.spec, .reset = TRUE)} with no knobs clears the
per-spec override entirely (the spec then falls through to
\code{\link[=set_preset]{set_preset()}} or \code{preset_spec()} defaults at render time).

\strong{Direct \code{preset_spec()} calls bypass lowering.} The five
named-list knobs are no longer slots on the \code{preset_spec} S7
class — they exist only as \code{preset()} / \code{set_preset()} arguments
that lower into \verb{@style}. \code{preset_spec(rules = list(...))}
(and analogous direct calls) raise "unused argument". Wrap such
calls in \code{tabular(...) |> preset(...)} so the lowering helper
fires and the layers land on \verb{@style}.

\strong{Cascade with \code{set_preset()}.} The engine resolves the active
preset in this order: (1) the spec's per-call preset (this verb),
(2) the session default attached via \code{\link[=set_preset]{set_preset()}},
(3) \code{preset_spec()} factory defaults. The first non-NULL layer
wins; layers are not field-merged across the cascade.
}
\examples{
# ---- Example 1: Landscape A4 for a wide efficacy table ----
#
# BOR table where the four-arm column block fits portrait letter
# with a smaller body font, but the sponsor wants A4 landscape at
# 8pt for visual breathing room. `preset()` attaches the geometry;
# `paginate()` reads it later to size the per-page row budget.
bor_levels <- c(
  "CR", "PR", "SD", "NON-CR/NON-PD", "PD", "NE", "MISSING",
  "ORR (CR + PR)", "CBR (CR + PR + SD)",
  "DCR (CR + PR + SD + NON-CR/NON-PD)", "95\% CI (Clopper-Pearson)"
)
eff <- cdisc_eff_resp
eff$stat_label <- factor(eff$stat_label, levels = bor_levels)
ne <- stats::setNames(cdisc_eff_n$n, cdisc_eff_n$arm_short)

tabular(
  eff,
  titles = c(
    "Table 14.2.1",
    "Best Overall Response and Response Rates",
    "Efficacy Evaluable Population"
  ),
  footnotes = "Response per RECIST 1.1, investigator assessment."
) |>
  cols(
    stat_label  = col_spec(label = "Response"),
    row_type    = col_spec(visible = FALSE),
    groupid     = col_spec(visible = FALSE),
    group_label = col_spec(visible = FALSE),
    placebo    = col_spec(label = "Placebo\nN={ne['placebo']}"),
    drug_50    = col_spec(label = "Drug 50\nN={ne['drug_50']}"),
    drug_100   = col_spec(label = "Drug 100\nN={ne['drug_100']}")
  ) |>
  sort_rows(by = c("groupid", "stat_label")) |>
  preset(
    orientation = "landscape",
    paper_size  = "a4",
    font_size   = 8
  ) |>
  paginate()

# ---- Example 2: Per-spec override with per-page chrome ----
#
# The submission session sets a portrait letter 9pt default (typical
# safety-table geometry). One particular AE table needs landscape
# for a long PT label band; the per-spec `preset()` overrides only
# orientation. The same per-spec call wires the canonical
# per-page header band (protocol on the left, page X of Y on the
# right) and a footer band that auto-resolves the calling
# script's name and the current render timestamp via the
# `{program}` and `{datetime}` tokens.
set_preset(font_size = 9, paper_size = "letter")

n <- stats::setNames(cdisc_saf_n$n, cdisc_saf_n$arm_short)

tabular(
  cdisc_saf_aesocpt,
  titles = c(
    "Table 14.3.1",
    "Adverse Events by System Organ Class and Preferred Term",
    "Safety Population"
  ),
  footnotes = "Subjects are counted once per SOC and once per PT."
) |>
  cols(
    label    = col_spec(label = "SOC / PT", indent = "indent_level"),
    soc      = col_spec(usage = "group", visible = FALSE,
                        group_display = "column_repeat"),
    row_type = col_spec(visible = FALSE),
    soc_n    = col_spec(visible = FALSE),
    n_total  = col_spec(visible = FALSE),
    placebo  = col_spec(label = "Placebo\nN={n['placebo']}"),
    drug_50  = col_spec(label = "Drug 50\nN={n['drug_50']}"),
    drug_100 = col_spec(label = "Drug 100\nN={n['drug_100']}"),
    Total    = col_spec(label = "Total\nN={n['Total']}")
  ) |>
  headers("Treatment Group" = c("placebo", "drug_50", "drug_100", "Total")) |>
  sort_rows(by = c("soc_n", "n_total"), descending = c(TRUE, TRUE)) |>
  preset(
    orientation = "landscape",
    pagehead = list(
      left  = "Protocol: ABC-123",
      right = "Page {page} of {npages}"
    ),
    pagefoot = list(
      left  = "{program}",
      right = "{datetime}"
    )
  ) |>
  paginate(keep_together = "soc")

# Reset the session default so subsequent examples / R sessions
# are not affected.
set_preset(.reset = TRUE)

}
\seealso{
\strong{Session-scope partners:} \code{\link[=set_preset]{set_preset()}}, \code{\link[=get_preset]{get_preset()}}.

\strong{Render-geometry consumer:} \code{\link[=paginate]{paginate()}} derives the per-page
row budget from the active preset's paper, orientation, margins,
and font size.

\strong{Sibling build verbs:} \code{\link[=cols]{cols()}} / \code{\link[=col_spec]{col_spec()}},
\code{\link[=headers]{headers()}}, \code{\link[=sort_rows]{sort_rows()}}, \code{\link[=style]{style()}}.

\strong{Entry / terminal verbs:} \code{\link[=tabular]{tabular()}}, \code{\link[=emit]{emit()}},
\code{\link[=as_grid]{as_grid()}}.
}
