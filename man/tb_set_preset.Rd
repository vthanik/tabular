% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tb_preset.R
\name{tb_set_preset}
\alias{tb_set_preset}
\title{Set a session-wide rendering preset (ggplot2 \code{theme_set()} style)}
\usage{
tb_set_preset(..., reset = FALSE)
}
\arguments{
\item{...}{Named geometry / styling values. Same accepted keys as
\code{\link[=tb_preset]{tb_preset()}}.}

\item{reset}{Logical. \code{TRUE} clears the session default back to
built-in defaults (ignores the \code{...} values).}
}
\value{
Invisibly returns the previous session preset, so you can
restore it after a scoped change:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{old <- tb_set_preset(font_size = 8)
on.exit(tb_set_preset(!!!old), add = TRUE)
}\if{html}{\out{</div>}}
}
\description{
Sets a session-scope default preset that every subsequent
\code{\link[=tb_table]{tb_table()}} call inherits at construction time. Per-table
overrides via \code{\link[=tb_preset]{tb_preset()}} (the pipe form) compose on top.
}
\details{
The session preset lives in a package environment and persists for
the lifetime of the R session. It is \strong{not} saved across sessions
and is not file-discovered -- the package has no YAML / config-file
magic.
}
\section{Inspecting the active default}{

Call \code{\link[=tb_get_preset]{tb_get_preset()}} to retrieve the current session default.
}

\section{Errors}{

Raises \code{tabular_error_runtime} while the implementation is still
stubbed. Phase 1b lifts the stub.
}

\seealso{
\code{\link[=tb_preset]{tb_preset()}} for the per-table override.

Other defaults: 
\code{\link{tb_get_preset}()},
\code{\link{tb_preset}()}
}
\concept{defaults}
