% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/footnote.R
\name{footnote}
\alias{footnote}
\title{Attach an auto-numbered footnote to a table location}
\usage{
footnote(.spec, text, .at = cells_body(), id = NULL, symbol = NULL)
}
\arguments{
\item{.spec}{\emph{The \code{tabular_spec} to annotate.} \verb{<tabular_spec>: required}.}

\item{text}{\emph{The footnote text.} \verb{<character(1)> | md() | html()}.
Wrap in \code{\link[=md]{md()}} / \code{\link[=html]{html()}} for inline markup; plain strings are
shown verbatim. A plain string supports glue-style \code{{expr}}
interpolation, evaluated as R code in the calling environment at
build time (double a brace for a literal one); an \code{md()} / \code{html()}
value is passed through without interpolation.}

\item{.at}{\emph{Where the marker is placed.} \verb{<tabular_location>: default [}cells_body()\verb{]}. Any \verb{cells_*()} location: a body-cell predicate
(\code{cells_body(where = ...)}), a column header (\code{cells_headers()}), a
title line (\code{cells_title()}), and so on.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# data-driven body anchor: mark every high-frequency preferred term
footnote(spec, "Includes events of any severity.",
         .at = cells_body(where = n_total >= 50, j = "label"))

# column-header anchor: mark the analysis-population denominator
footnote(spec, "Safety population.",
         .at = cells_headers(j = "Total"))
}\if{html}{\out{</div>}}

\strong{Note:} the styling argument is \code{.at}, never \code{at}.}

\item{id}{\emph{Stable identifier for sharing one marker across anchors.}
\verb{<character(1)> | NULL}. Two \code{footnote()} calls with the same \code{id}
share a single marker and a single note line. \code{NULL} (default)
makes each call its own note.}

\item{symbol}{\emph{Pin an explicit marker glyph.} \verb{<character(1)> | NULL}.
Overrides the auto-allocated marker for this note (e.g. \code{"*"}). A
pinned symbol is reserved and skipped by the auto-allocator, so it
never collides. \code{NULL} (default) auto-allocates from the preset
scheme.}
}
\value{
\emph{A \code{tabular_spec}.} Pipe it onward to more verbs or to
\code{\link[=emit]{emit()}}.
}
\description{
Anchor a footnote to a cell, column header, title line, or any other
\verb{cells_*()} location. The engine assigns the marker, places a
superscript at every matching anchor, and emits the marked-footnote
line at the foot of the table. Markers are assigned \strong{once}, in
reading order, deduped by \code{id}, and are byte-identical across every
backend (RTF / LaTeX / PDF / HTML / DOCX) and every page, so the
marker at the anchor can never desynchronise from its note.
}
\details{
\strong{Engine-assigned, never hand-typed.} Unlike a literal \verb{^a^} typed
into both a cell and the \code{footnotes} argument, a \code{footnote()} marker
is allocated by the resolve engine after decimal alignment, so it
never disturbs column alignment and never drifts out of sync. The
scheme (\code{letters} / \code{numbers} / \code{symbols}) and the block-line format
come from the active preset (\code{footnote_markers}, \code{footnote_label}).

\strong{Dedup by id.} Give two anchors the same \code{id} to share one marker
and one note line. Without an \code{id}, each \code{footnote()} call is its own
note.

\strong{Coexists with \code{footnotes}.} Manual \code{footnotes} lines render first;
the auto-numbered block follows. The two systems do not cross-dedup,
so do not mix a hand-typed marker with an engine one for the same note.
}
\examples{
# ---- Example 1: a denominator note on a column header ----
#
# AE-by-SOC/PT table whose Total column header carries the analysis-
# population note. The engine drops a superscript "a" on the header
# and prints "a <text>" beneath the table.
n <- stats::setNames(cdisc_saf_n$n, cdisc_saf_n$arm_short)
tabular(cdisc_saf_aesocpt) |>
  cols(
    label    = col_spec(label = "SOC / PT", indent_by = "indent_level"),
    soc      = col_spec(visible = FALSE),
    row_type = col_spec(visible = FALSE),
    soc_n    = col_spec(visible = FALSE),
    n_total  = col_spec(visible = FALSE),
    placebo  = col_spec(label = "Placebo\nN={n['placebo']}"),
    drug_100 = col_spec(label = "Drug 100\nN={n['drug_100']}"),
    Total    = col_spec(label = "Total\nN={n['Total']}")
  ) |>
  footnote(
    "Safety population: all randomised subjects who took study drug.",
    .at = cells_headers(j = "Total")
  )

# ---- Example 2: a data-driven note shared across cells ----
#
# A single note marks every high-frequency preferred term (n >= 50 in
# the Total column) in the SOC/PT stub. Sharing one `id` keeps it to
# one marker and one line; the marker lands on each matching cell.
tabular(cdisc_saf_aesocpt) |>
  cols(
    label    = col_spec(label = "SOC / PT", indent_by = "indent_level"),
    soc      = col_spec(visible = FALSE),
    row_type = col_spec(visible = FALSE),
    soc_n    = col_spec(visible = FALSE),
    n_total  = col_spec(visible = FALSE),
    placebo  = col_spec(label = "Placebo"),
    drug_100 = col_spec(label = "Drug 100"),
    Total    = col_spec(label = "Total")
  ) |>
  footnote(
    md("Includes events of *any* severity."),
    .at = cells_body(where = n_total >= 50, j = "label"),
    id = "anysev"
  )

}
\seealso{
\strong{Manual footnote lines:} the \code{footnotes} argument to \code{\link[=tabular]{tabular()}}.

\strong{Location helpers:} \code{\link[=cells_body]{cells_body()}}, \code{\link[=cells_headers]{cells_headers()}},
\code{\link[=cells_title]{cells_title()}}.

\strong{Inline markup:} \code{\link[=md]{md()}}, \code{\link[=html]{html()}}.
}
