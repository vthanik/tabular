% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_across.R
\name{pivot_across}
\alias{pivot_across}
\title{Convert a cards ARD to a wide display data.frame}
\usage{
pivot_across(
  data,
  statistic = list(continuous = "{mean} ({sd})", categorical = "{n} ({p}\%)"),
  column = NULL,
  row_group = NULL,
  label = NULL,
  overall = "Total",
  decimals = NULL,
  fmt = NULL,
  aux = NULL
)
}
\arguments{
\item{data}{\emph{Long ARD input data.}
\verb{<data.frame>: required}. At minimum needs \code{stat_name} and
\code{stat}. Cards-style group columns (\code{group1}, \code{group1_level},
...) and \code{variable} / \code{variable_level} are auto-detected.
Tibbles / \code{card} objects / arrow tables are coerced via
\code{as.data.frame()}.}

\item{statistic}{\emph{Format spec for cell composition.}
\verb{<character(1) | named list>: required}. Combines one or more
ARD stats into one display cell. Three accepted forms — each
illustrated below. Inside a format string, \code{{stat_name}}
substitutes that stat's value from the ARD (for example,
\code{"{n} ({p}\%)"} interpolates the \code{n} and \code{p} stats into a
\code{"53 (62\%)"} cell). The lookup order when a value is needed
for a variable is: per-variable -> per-context -> \code{default}
-> the literal \code{"{n}"}.
\subsection{Form 1: single string}{

One format string applied to every variable regardless of
context. Use when your ARD is homogeneous (e.g. all
categorical).

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Every variable rendered as "n (p\%)" — categorical-only slice.
cat_only <- cdisc_saf_demo_ard[cdisc_saf_demo_ard$context == "categorical", ]
pivot_across(
  cat_only,
  statistic = "\{n\} (\{p\}\%)"
)
}\if{html}{\out{</div>}}
}

\subsection{Form 2: named list by context}{

Different formats per context. This is the typical clinical-table
form because demographics mix continuous and categorical
variables.

\strong{The list names must match the values in the ARD's \code{context}
column verbatim.} Which strings appear there depends on how the
ARD was built:
\itemize{
\item \code{cards::ard_continuous()} / \code{ard_categorical()} emit
\code{"continuous"} / \code{"categorical"}.
\item \code{cards::ard_summary()} / \code{ard_tabulate()} emit \code{"summary"} /
\code{"tabulate"}.
}

So an ARD assembled with \code{ard_stack(ard_summary(...), ard_tabulate(...))} is keyed \code{summary} / \code{tabulate}, not
\code{continuous} / \code{categorical}. Inspect \code{unique(ard$context)} when
unsure.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# AGE (continuous) -> "75.2 (8.59)"; SEX (categorical) -> "53 (62\%)"
pivot_across(
  cdisc_saf_demo_ard,
  statistic = list(
    continuous  = "\{mean\} (\{sd\})",
    categorical = "\{n\} (\{p\}\%)"
  )
)
}\if{html}{\out{</div>}}
}

\subsection{Form 3: named list by variable}{

Override on a per-variable basis; fall back to \code{default} or
context. Use when one variable needs a custom format.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# AGE shows just the mean; SEX / RACE keep the categorical default.
pivot_across(
  cdisc_saf_demo_ard,
  statistic = list(
    AGE         = "\{mean\}",
    categorical = "\{n\} (\{p\}\%)",
    default     = "\{mean\} (\{sd\})"
  )
)
}\if{html}{\out{</div>}}
}

\subsection{Multi-row continuous spec}{

Any single entry can itself be a \strong{named character vector} —
each element becomes one display row, with the name as the row
label. Use for \verb{N / Mean (SD) / Median / Min, Max}-style blocks.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{pivot_across(
  cdisc_saf_demo_ard,
  statistic = list(
    continuous = c(
      N           = "\{N\}",
      "Mean (SD)" = "\{mean\} (\{sd\})",
      Median      = "\{median\}",
      "Min, Max"  = "\{min\}, \{max\}"
    ),
    categorical = "\{n\} (\{p\}\%)"
  )
)
}\if{html}{\out{</div>}}
}}

\item{column}{\emph{What runs across the top of the table.}
\verb{<character | NULL>: default NULL}. A single grouping variable
whose unique values become arm columns (\code{NULL} auto-detects from
\code{group1}, or picks the single non-standard column of renamed
input). Two reserved tokens turn the analysis variable into a
\strong{column band}:
\itemize{
\item \code{c(".variable", "<arm>")} — each variable becomes a band of
arm columns; the statistic entries stack as rows. The cells
are combined strings (e.g. \code{"323.9 (106)"}). Emitted columns
are named \code{"<variable>..<arm>"}.
\item \code{c(".variable", ".stat")} — each variable becomes a band whose
statistic entries become their own columns (the landscape
"value and change" shell); the arm drops to a leading row
stub. Emitted columns are named \code{"<variable>..<stat-entry>"}.
}

Anything present in the ARD but not named in \code{column} (and not in
\code{row_group}) stacks as rows. Per-variable \code{statistic} / \code{decimals}
resolve inside each band, so bands may carry different (even
different-length) stat lists; ragged bands pad with \code{NA}.

\strong{Tip:} you reference the emitted column names verbatim in a
manual \code{\link[=headers]{headers()}} call to build the band spanners; pivot_across
does not build spanners itself.}

\item{row_group}{\emph{Second, non-column grouping dimension.}
\verb{<character(1) | NULL>: default NULL}. Names the non-arm group
variable of a two-variable \code{.by} (e.g. \code{SEX} in
\code{ard_stack(.by = c(ARM, SEX))}). It widens into a leading row
column (not a pivoted arm column), so the result composes with
\code{\link[=subgroup]{subgroup(by = ...)}} or
\code{\link[=group_rows]{group_rows()}} downstream.

\strong{Why it is required.} cards encodes a crossing factor and a
SOC/PT hierarchy identically (the second group variable appears
in \code{variable} on its by-marginal rows), so the two cannot be told
apart automatically. Naming \code{row_group} declares "this is a
crossing factor": the by-marginal rows are dropped and the flat
path is used. Leave it \code{NULL} for a genuine hierarchy.

\strong{Restriction:} Must name a second grouping variable present in
the ARD and must differ from \code{column}.}

\item{label}{\emph{Variable-name to display-label map.}
\verb{<character> | NULL: default NULL}. Named character vector
mapping variable names to display labels (e.g.
\code{c(AGE = "Age (years)", SEX = "Sex")}). Applies to
\code{variable}, \code{soc}, and \code{label} columns of the output. \code{NULL}
leaves the upstream variable names verbatim.

\strong{Renaming the hierarchical "overall" row.} A
\code{cards::ard_stack_hierarchical(overall = TRUE)} ARD carries an
internal \code{..ard_hierarchical_overall..} sentinel for the
grand-total ("any event") row. It is relabelled to \code{"Overall"}
by default; map the sentinel key to override, e.g.
\code{label = c("..ard_hierarchical_overall.." = "TOTAL SUBJECTS WITH AN EVENT")}.
The raw sentinel never reaches the output at any hierarchy depth.}

\item{overall}{\emph{Column name for \code{NA}-arm (overall / total) rows.}
\verb{<character(1) | NULL>: default "Total"}. Pass \code{NULL} to drop
overall rows entirely (per-arm only output).

\strong{Requirement:} this relabels pooled rows the ARD already
carries — the \code{NA}-arm rows cards emits from
\code{cards::ard_stack_hierarchical(overall = TRUE)} or an
\code{ard_*(.overall = TRUE)}. It does not synthesize a total: cards
re-runs the calculation with the \code{by} variable removed, so the
pooled \code{n} / \code{N} / \code{p} stay internally consistent. With no such
rows in the input there is no overall column to label.

\strong{Note:} if a study arm is literally named the same as \code{overall}
(default \code{"Total"}), that arm and the pooled rows collide under
one label and the pivot warns. Pass a distinct \verb{overall =} or
rename the arm upstream.}

\item{decimals}{\emph{Per-stat decimal precision.}
\verb{<named integer | named list>: default }c()``. Accepts three
forms:
\itemize{
\item \strong{named integer vector} — global per-stat overrides
(\code{c(mean = 1, sd = 2, p = 0)}).
\item \strong{named list keyed by variable} — per-variable plus \code{.default}
(\code{list(AGE = c(mean = 2), .default = c(p = 1))}).
\item \strong{named list keyed by \code{row_group} value} — per-group precision in
one call (\code{list(SYSBP = c(mean = 0, sd = 1), WEIGHT = c(mean = 1), .default = c(mean = 1, sd = 2))}). Each entry is a per-token spec (a
named numeric vector, or a bare scalar applied to every token).
}

Built-in defaults apply when none sets a stat.

\strong{Interaction:} a list \code{decimals} is read as per-\code{row_group} only when
\code{row_group} is set and every key (apart from \code{.default}) is one of its
levels; otherwise it stays per-variable. Within the matched group the
token falls back group token, then the per-group \code{.default} token, then
the built-in default. A group present in the data but absent from the
list (and NA / ungrouped rows) uses \code{.default}. If \code{row_group} is \code{NULL}
but the keys match no variable, \code{pivot_across()} errors and asks for a
\code{row_group}.}

\item{fmt}{\emph{Per-stat custom formatter functions.}
\verb{<named list of function>: default }list()``. Each function
takes a numeric value and returns a character string;
overrides built-ins and \code{decimals} for that stat. Useful for
p-value styling and other domain-specific formatting.}

\item{aux}{\emph{Auxiliary comparison columns from a second ARD.}
\verb{<named list | NULL>: default NULL}. Each entry is a between-arm
statistic (difference, hazard ratio, p-value, ...) computed in its
own ARD and bound on as a trailing column, aligned 1:1 on the
\code{row_group} key. The entry name is the column name; reference it in
a manual \code{\link[=headers]{headers()}} call. Each entry is a list:
\itemize{
\item \code{ard} — the auxiliary ARD (required), one row per \code{row_group}.
\item \code{statistic} — its format string (e.g.
\code{"{estimate} ({conf.low}, {conf.high})"}); defaults to \code{"{n}"}.
\item \code{decimals} / \code{fmt} — optional, as for the main pivot.
}

Entries append left to right. One entry is one column; for several
comparison columns (estimate then p-value) pass several entries.

\strong{Requirement:} needs a \code{row_group}; the auxiliary rows align to
the main table on it, which must be unique 1:1 on both sides (a
many-to-many key would silently fabricate rows, so it aborts).

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# p-value formatter: render below-threshold values as "<0.001".
fmt = list(
  p.value = function(x) \{
    ifelse(x < 0.001, "<0.001", sprintf("\%.3f", x))
  \}
)
}\if{html}{\out{</div>}}}
}
\value{
\emph{A wide \code{data.frame} ready for \code{\link[=tabular]{tabular()}}.} Schema:
\itemize{
\item \code{variable} — variable name (or label after \code{label = ...}).
\item \code{stat_label} — display-row label.
\item One column per arm level (named after the \code{group1_level}
values or the renamed arm column).
\item \code{Total} (or whatever \code{overall} is set to) when applicable.
\item A leading column named after \code{row_group} when set (the second
grouping dimension).
\item Hierarchical ARD adds \code{soc}, \code{label}, \code{row_type} instead of
\code{variable}.
}

Pass the result straight into \code{\link[=tabular]{tabular()}} to start the
render pipeline.
}
\description{
\code{pivot_across()} is tabular's input-side helper: it consumes a
long Analysis Results Data (ARD) data frame (typically produced by
\code{cards::ard_stack()} or \code{cards::ard_stack_hierarchical()}) and
returns a wide display data.frame ready to pass to \code{tabular()}.
}
\details{
tabular's package boundary is \strong{display-only}: pre-summarised
data in, rendered file out. \code{pivot_across()} is the canonical
bridge between the cards aggregation backend and that boundary.
It does not aggregate — it pivots arms to columns, interpolates
per-cell display strings from the stat values, and applies
decimal precision. Filtering, weighting, and aggregation happen
upstream in cards or your own data-prep step.
\subsection{Key \code{statistic} by the ARD \code{context}}{

\code{statistic} (and \code{fmt}) are matched against the ARD's \code{context}
column verbatim, and that value differs per generating function.
Keying by the wrong name silently drops the format. Inspect
\code{unique(ard$context)} first and key to match (or pass a single
format string / \verb{default =} to cover everything). When an
explicitly-supplied \code{statistic} matches no context at all,
\code{pivot_across()} warns rather than silently emitting \code{{n}}.\tabular{ll}{
   Generating function \tab \code{context} to key on \cr
   \code{cards::ard_summary()} \tab \code{summary} \cr
   \code{cards::ard_tabulate()} \tab \code{tabulate} \cr
   \code{cards::ard_continuous()} \tab \code{continuous} \cr
   \code{cards::ard_categorical()} \tab \code{categorical} \cr
   \code{cards::ard_stack_hierarchical()} \tab \code{tabulate} + \code{hierarchical} \cr
   \code{cardx::ard_categorical_ci()} \tab \code{proportion_ci} \cr
   \code{cardx::ard_continuous_ci()} \tab \code{continuous_ci} \cr
}

}

\subsection{Zero-suppression (always-on default)}{

A row whose \code{n} value equals zero renders the whole cell as the
bare \code{n} value instead of fully interpolating the format string.
For a categorical level with \code{n = 0}, the cell shows \code{"0"}, not
\code{"0 (0.0\%)"}. This is clinical convention — empty cells should
read as a single zero, not advertise a meaningless rate.

\strong{How the default fires (chain of events).} During cell assembly,
before format-string interpolation, the engine checks the row's
\code{n} stat. If it is zero, the engine short-circuits and returns the
formatted \code{n} value (\code{"0"}) as the entire cell — \code{{p}} is never
substituted, so the \verb{(0.0\%)} half of the format string is dropped.

\strong{How to opt out: supply a custom \code{fmt$n}.} Setting any function
under \code{fmt$n} is the engine's signal that the user owns the \code{n}
rendering. The short-circuit is disabled for the whole table; for
every row the full format string interpolates, so \code{{n}} becomes
your formatter's output and \code{{p}} becomes the standard percentage.
For \code{n = 0}, that's \code{"0 (0.0\%)"}.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Force "0 (0.0\%)" for n = 0 rows by attaching a custom n formatter.
# The body of fmt$n can be the default integer rendering — its
# presence alone is what disables the zero-suppression branch.
pivot_across(
  cdisc_saf_demo_ard,
  statistic = list(
    continuous  = "\{mean\} (\{sd\})",
    categorical = "\{n\} (\{p\}\%)"
  ),
  fmt = list(n = function(x) sprintf("\%d", as.integer(x)))
)
}\if{html}{\out{</div>}}
}

\subsection{Pharma rounding (always-on default)}{

A percentage that would otherwise round to \code{0} (when the value
is positive but smaller than the chosen precision) renders as
\verb{<0.1}; one that would round to \code{100} (positive but smaller than
100) renders as \verb{>99.9}. The threshold is precision-aware:
\code{decimals = c(p = 2)} produces \verb{<0.01} / \verb{>99.99}. This matches
the pharma convention of never claiming exactly \verb{0\%} or \verb{100\%}
when at least one subject contributed.

Override per-stat via \code{fmt}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Show exact rounded percentages even at the extremes
pivot_across(
  data,
  statistic = "\{n\} (\{p\}\%)",
  decimals  = c(p = 1),
  fmt = list(p = function(x) sprintf("\%.1f", x * 100))
)
}\if{html}{\out{</div>}}

Your \code{fmt$p} receives the raw stat value (a proportion between
0 and 1) and returns the displayed string. The pharma-threshold
branch only fires inside the built-in \code{p} formatter and the
\code{decimals}-driven path, so any custom \code{fmt$p} bypasses it.
}
}
\examples{
# ---- Example 1: Demographics — long ARD to rendered spec ----
#
# Full pipeline from a `cards::ard_stack()`-style long ARD to a
# sorted `tabular_spec`. The multi-row continuous block (N /
# Mean (SD) / Median / Min, Max) sits above each categorical
# block; decimals are set per-stat (mean 1, sd 2, p 1) to match
# the CDISC convention.
n <- stats::setNames(cdisc_saf_n$n, cdisc_saf_n$arm_short)

cdisc_saf_demo_ard |>
  pivot_across(
    statistic = list(
      continuous = c(
        N           = "{N}",
        "Mean (SD)" = "{mean} ({sd})",
        Median      = "{median}",
        "Min, Max"  = "{min}, {max}"
      ),
      categorical = "{n} ({p}\%)"
    ),
    decimals = c(mean = 1, sd = 2, p = 1, median = 1, min = 0, max = 0),
    label    = c(AGE = "Age (years)", SEX = "Sex", RACE = "Race")
  ) |>
  tabular(
    titles = c(
      "Table 14.1.1",
      "Demographics and Baseline Characteristics",
      "Safety Population"
    ),
    footnotes = "Percentages based on N per treatment group."
  ) |>
  cols(
    variable   = col_spec(label = "Parameter"),
    stat_label = col_spec(label = "Statistic"),
    Placebo    = col_spec(
      label = "Placebo\nN={n['placebo']}",
      align = "decimal"
    ),
    `Xanomeline Low Dose` = col_spec(
      label = "Drug 50\nN={n['drug_50']}",
      align = "decimal"
    ),
    `Xanomeline High Dose` = col_spec(
      label = "Drug 100\nN={n['drug_100']}",
      align = "decimal"
    ),
    Total = col_spec(
      label = "Total\nN={n['Total']}",
      align = "decimal"
    )
  )

# ---- Example 2: Hierarchical SOC/PT AE table ----
#
# Hierarchical `cards::ard_stack_hierarchical()` output threaded
# through `pivot_across()`. The hierarchical ARD emits a
# (soc, label, row_type) triple plus one stat row per (arm, SOC, PT);
# `pivot_across()` folds the arm dimension to columns and preserves
# the hierarchy markers. Derive `indent_level` from `row_type` so
# `col_spec(indent = "indent_level")` drives the SOC -> PT
# indent on the `label` column.
wide <- cdisc_saf_aesocpt_ard |>
  pivot_across(statistic = "{n} ({p}\%)")
wide$indent_level <- as.integer(wide$row_type == "pt")

tabular(
  wide,
  titles = c(
    "Table 14.3.1",
    "Adverse Events by System Organ Class and Preferred Term",
    "Safety Population"
  ),
  footnotes = c(
    "Subjects are counted once per SOC and once per PT.",
    "Percentages based on N per treatment group."
  )
) |>
  cols(
    label    = col_spec(label = "SOC / PT", indent = "indent_level"),
    soc      = col_spec(visible = FALSE),
    row_type = col_spec(visible = FALSE),
    Placebo  = col_spec(
      label = "Placebo\nN={n['placebo']}",
      align = "decimal"
    ),
    `Xanomeline Low Dose` = col_spec(
      label = "Drug 50\nN={n['drug_50']}",
      align = "decimal"
    ),
    `Xanomeline High Dose` = col_spec(
      label = "Drug 100\nN={n['drug_100']}",
      align = "decimal"
    )
  )

# ---- Example 3: Hierarchical ARD (SOC / PT) ----
#
# `cdisc_saf_aesocpt_ard` carries an `ard_stack_hierarchical` shape with
# two grouping variables (AEBODSYS / AEDECOD). `pivot_across()`
# recognises the hierarchical structure and emits dedicated `soc`,
# `label`, and `row_type` columns so the SOC -> PT nesting survives
# the pivot. The result is ready for `tabular()` plus `sort_rows()`.
head(cdisc_saf_aesocpt_ard, 3)

wide <- cdisc_saf_aesocpt_ard |>
  pivot_across(statistic = "{n} ({p}\%)")
head(wide, 3)

# ---- Example 4: Multi-row continuous spec + label re-labelling ----
#
# `statistic = c(<label> = <template>, ...)` produces one display
# row per named entry — the canonical "N / Mean (SD) / Median /
# Min, Max" block for continuous variables. `label = c(...)`
# renames the variable headings emitted into the wide output.
cdisc_saf_demo_ard |>
  pivot_across(
    statistic = list(
      continuous = c(
        N            = "{N}",
        "Mean (SD)"  = "{mean} ({sd})",
        Median       = "{median}",
        "Q1, Q3"     = "{p25}, {p75}",
        "Min, Max"   = "{min}, {max}"
      ),
      categorical = "{n} ({p}\%)"
    ),
    label = c(
      AGE    = "Age (years)",
      WEIGHT = "Weight (kg)",
      HEIGHT = "Height (cm)",
      BMI    = "BMI (kg/m^2)"
    )
  )

# ---- Example 5: ARD keyed by summary / tabulate contexts ----
#
# The `statistic` list names must match the ARD's `context` column
# verbatim. `cards::ard_summary()` / `ard_tabulate()` emit `"summary"` /
# `"tabulate"` (not the `"continuous"` / `"categorical"` of
# `ard_continuous()` / `ard_categorical()`), so a list keyed
# `continuous`/`categorical` would silently match nothing. Always check
# `unique(ard$context)` first. Here the bundled `cdisc_saf_demo_ard` is
# relabelled to mimic `ard_summary()` + `ard_tabulate()` output; the
# by-variable's own row drops automatically and both the summary and
# the tabulate variables survive.
card_st <- cdisc_saf_demo_ard
card_st$context[card_st$context == "continuous"] <- "summary"
card_st$context[card_st$context == "categorical"] <- "tabulate"
pivot_across(
  card_st,
  statistic = list(
    summary  = "{mean} ({sd})",
    tabulate = "{n} ({p}\%)"
  )
)

# ---- Example 6: Analysis variables as side-by-side column bands ----
#
# `column = c(".variable", "<arm>")` turns each analysis variable into
# its own band of arm columns (the "value and change side by side"
# shape), with statistics stacked as rows. Per-variable `statistic` /
# `decimals` resolve inside each band. Emitted columns are named
# "<variable>..<arm>"; reference them verbatim in a manual `headers()`
# call to draw the band spanners (pivot_across never builds spanners).
vitals <- cdisc_saf_demo_ard[
  cdisc_saf_demo_ard$variable \%in\% c("AGE", "WEIGHT"),
]
vitals |>
  pivot_across(
    column = c(".variable", "TRT01A"),
    overall = NULL,
    statistic = list(
      AGE    = c(N = "{N}", "Mean (SD)" = "{mean} ({sd})", Median = "{median}"),
      WEIGHT = c(N = "{N}", "Mean (SD)" = "{mean} ({sd})", Median = "{median}")
    ),
    decimals = list(
      AGE    = c(mean = 1, sd = 2, median = 1),
      WEIGHT = c(mean = 1, sd = 2, median = 1)
    )
  ) |>
  tabular(
    titles = c(
      "Table 14.2.1",
      "Summary of Continuous Parameters by Treatment",
      "Safety Population"
    )
  ) |>
  cols(stat_label = col_spec(label = "Statistic")) |>
  headers(
    Age = c(
      "AGE..Placebo",
      "AGE..Xanomeline Low Dose",
      "AGE..Xanomeline High Dose"
    ),
    Weight = c(
      "WEIGHT..Placebo",
      "WEIGHT..Xanomeline Low Dose",
      "WEIGHT..Xanomeline High Dose"
    )
  )

# ---- Example 7: Statistics as columns within each band ----
#
# `column = c(".variable", ".stat")` spreads each statistic entry into
# its own column (the landscape shell); the arm drops to a leading row
# stub. Emitted columns are named "<variable>..<stat-entry>". Bands may
# carry different stat sets, with no row-alignment needed.
vitals |>
  pivot_across(
    column = c(".variable", ".stat"),
    overall = NULL,
    statistic = list(
      AGE    = c(N = "{N}", Mean = "{mean}", SD = "{sd}"),
      WEIGHT = c(N = "{N}", Mean = "{mean}", SD = "{sd}", Median = "{median}")
    ),
    decimals = c(mean = 1, sd = 2, median = 1)
  )

# ---- Example 8: Per-row-group decimal precision ----
#
# A by-parameter vitals table where each parameter carries its own
# value precision: systolic BP to 0 dp, weight to 1 dp, in ONE call.
# `decimals` is keyed by the `row_group` (PARAM) value; the engine
# selects each row's token precision by its parameter. No bundled ARD
# carries a second grouping dimension, so build a tiny one inline.
vital_ard <- do.call(rbind, lapply(
  list(
    c("SYSBP", "Placebo", "mean", "133.27"),
    c("SYSBP", "Placebo", "sd", "15.81"),
    c("SYSBP", "Drug", "mean", "128.94"),
    c("SYSBP", "Drug", "sd", "14.02"),
    c("WEIGHT", "Placebo", "mean", "71.43"),
    c("WEIGHT", "Placebo", "sd", "12.77"),
    c("WEIGHT", "Drug", "mean", "73.06"),
    c("WEIGHT", "Drug", "sd", "13.19")
  ),
  function(r) {
    data.frame(
      group1 = "PARAM",
      group1_level = r[[1]],
      group2 = "TRTA",
      group2_level = r[[2]],
      variable = "AVAL",
      variable_level = NA_character_,
      context = "continuous",
      stat_name = r[[3]],
      stat_label = r[[3]],
      stat = I(list(as.numeric(r[[4]]))),
      stringsAsFactors = FALSE
    )
  }
))
vital_ard |>
  pivot_across(
    column = "TRTA",
    row_group = "PARAM",
    overall = NULL,
    statistic = list(continuous = "{mean} ({sd})"),
    decimals = list(
      SYSBP = c(mean = 0, sd = 1),
      WEIGHT = c(mean = 1, sd = 2)
    )
  )

}
\seealso{
\strong{Pipeline entry consumer:} \code{\link[=tabular]{tabular()}} — wraps the wide data
frame this helper returns.

\strong{Downstream spec-build verbs:} \code{\link[=cols]{cols()}} / \code{\link[=col_spec]{col_spec()}},
\code{\link[=headers]{headers()}}, \code{\link[=sort_rows]{sort_rows()}}, \code{\link[=style]{style()}},
\code{\link[=paginate]{paginate()}}, \code{\link[=preset]{preset()}}.

\strong{Terminal verbs:} \code{\link[=emit]{emit()}}, \code{\link[=as_grid]{as_grid()}}.
}
