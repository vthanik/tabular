% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_across.R
\name{pivot_across}
\alias{pivot_across}
\title{Convert a cards ARD to a wide display data.frame}
\usage{
pivot_across(
  data,
  statistic = list(continuous = "{mean} ({sd})", categorical = "{n} ({p}\%)"),
  column = NULL,
  label = NULL,
  overall = "Total",
  decimals = NULL,
  fmt = NULL
)
}
\arguments{
\item{data}{\emph{Long ARD input data.}
\verb{<data.frame>: required}. At minimum needs \code{stat_name} and
\code{stat}. Cards-style group columns (\code{group1}, \code{group1_level},
...) and \code{variable} / \code{variable_level} are auto-detected.
Tibbles / \code{card} objects / arrow tables are coerced via
\code{as.data.frame()}.}

\item{statistic}{\emph{Format spec for cell composition.}
\verb{<character(1) | named list>: required}. Combines one or more
ARD stats into one display cell. Three accepted forms — each
illustrated below. Inside a format string, \code{{stat_name}}
substitutes that stat's value from the ARD (for example,
\code{"{n} ({p}\%)"} interpolates the \code{n} and \code{p} stats into a
\code{"53 (62\%)"} cell). The lookup order when a value is needed
for a variable is: per-variable -> per-context -> \code{default}
-> the literal \code{"{n}"}.
\subsection{Form 1: single string}{

One format string applied to every variable regardless of
context. Use when your ARD is homogeneous (e.g. all
categorical).

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Every variable rendered as "n (p\%)" — categorical-only slice.
cat_only <- saf_demo_card[saf_demo_card$context == "categorical", ]
pivot_across(
  cat_only,
  statistic = "\{n\} (\{p\}\%)"
)
}\if{html}{\out{</div>}}
}

\subsection{Form 2: named list by context}{

Different formats for continuous vs categorical contexts. This
is the typical clinical-table form because demographics mix
the two.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# AGE (continuous) -> "75.2 (8.59)"; SEX (categorical) -> "53 (62\%)"
pivot_across(
  saf_demo_card,
  statistic = list(
    continuous  = "\{mean\} (\{sd\})",
    categorical = "\{n\} (\{p\}\%)"
  )
)
}\if{html}{\out{</div>}}
}

\subsection{Form 3: named list by variable}{

Override on a per-variable basis; fall back to \code{default} or
context. Use when one variable needs a custom format.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# AGE shows just the mean; SEX / RACE keep the categorical default.
pivot_across(
  saf_demo_card,
  statistic = list(
    AGE         = "\{mean\}",
    categorical = "\{n\} (\{p\}\%)",
    default     = "\{mean\} (\{sd\})"
  )
)
}\if{html}{\out{</div>}}
}

\subsection{Multi-row continuous spec}{

Any single entry can itself be a \strong{named character vector} —
each element becomes one display row, with the name as the row
label. Use for \verb{N / Mean (SD) / Median / Min, Max}-style blocks.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{pivot_across(
  saf_demo_card,
  statistic = list(
    continuous = c(
      N           = "\{N\}",
      "Mean (SD)" = "\{mean\} (\{sd\})",
      Median      = "\{median\}",
      "Min, Max"  = "\{min\}, \{max\}"
    ),
    categorical = "\{n\} (\{p\}\%)"
  )
)
}\if{html}{\out{</div>}}
}}

\item{column}{\emph{Grouping column whose unique values become arms.}
\verb{<character(1) | NULL>: default NULL}. \code{NULL} auto-detects
from the ARD's \code{group1} value or — for renamed input — picks
the single non-standard column. Pass a string when multiple
group columns exist.}

\item{label}{\emph{Variable-name to display-label map.}
\verb{<character> | NULL: default NULL}. Named character vector
mapping variable names to display labels (e.g.
\code{c(AGE = "Age (years)", SEX = "Sex")}). Applies to
\code{variable}, \code{soc}, and \code{label} columns of the output. \code{NULL}
leaves the upstream variable names verbatim.}

\item{overall}{\emph{Column name for \code{NA}-arm (overall / total) rows.}
\verb{<character(1) | NULL>: default "Total"}. Pass \code{NULL} to drop
overall rows entirely (per-arm only output).}

\item{decimals}{\emph{Per-stat decimal precision.}
\verb{<named integer | named list>: default }c()``. Accepts two
forms:
\itemize{
\item \strong{named integer vector} — global per-stat overrides
(\code{c(mean = 1, sd = 2, p = 0)}).
\item \strong{named list} — per-variable plus \code{.default}
(\code{list(AGE = c(mean = 2), .default = c(p = 1))}).
}

Built-in defaults apply when neither sets a stat.}

\item{fmt}{\emph{Per-stat custom formatter functions.}
\verb{<named list of function>: default }list()``. Each function
takes a numeric value and returns a character string;
overrides built-ins and \code{decimals} for that stat. Useful for
p-value styling and other domain-specific formatting.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# p-value formatter: render below-threshold values as "<0.001".
fmt = list(
  p.value = function(x) \{
    ifelse(x < 0.001, "<0.001", sprintf("\%.3f", x))
  \}
)
}\if{html}{\out{</div>}}}
}
\value{
\emph{A wide \code{data.frame} ready for \code{\link[=tabular]{tabular()}}.} Schema:
\itemize{
\item \code{variable} — variable name (or label after \code{label = ...}).
\item \code{stat_label} — display-row label.
\item One column per arm level (named after the \code{group1_level}
values or the renamed arm column).
\item \code{Total} (or whatever \code{overall} is set to) when applicable.
\item Hierarchical ARD adds \code{soc}, \code{label}, \code{row_type} instead of
\code{variable}.
}

Pass the result straight into \code{\link[=tabular]{tabular()}} to start the
render pipeline.
}
\description{
\code{pivot_across()} is tabular's input-side helper: it consumes a
long Analysis Results Data (ARD) data frame (typically produced by
\code{cards::ard_stack()} or \code{cards::ard_stack_hierarchical()}) and
returns a wide display data.frame ready to pass to \code{tabular()}.
}
\details{
tabular's package boundary is \strong{display-only}: pre-summarised
data in, rendered file out. \code{pivot_across()} is the canonical
bridge between the cards aggregation backend and that boundary.
It does not aggregate — it pivots arms to columns, interpolates
per-cell display strings from the stat values, and applies
decimal precision. Filtering, weighting, and aggregation happen
upstream in cards or your own data-prep step.
\subsection{Zero-suppression (always-on default)}{

A row whose \code{n} value equals zero renders the whole cell as the
bare \code{n} value instead of fully interpolating the format string.
For a categorical level with \code{n = 0}, the cell shows \code{"0"}, not
\code{"0 (0.0\%)"}. This is clinical convention — empty cells should
read as a single zero, not advertise a meaningless rate.

\strong{How the default fires (chain of events).} During cell assembly,
before format-string interpolation, the engine checks the row's
\code{n} stat. If it is zero, the engine short-circuits and returns the
formatted \code{n} value (\code{"0"}) as the entire cell — \code{{p}} is never
substituted, so the \verb{(0.0\%)} half of the format string is dropped.

\strong{How to opt out: supply a custom \code{fmt$n}.} Setting any function
under \code{fmt$n} is the engine's signal that the user owns the \code{n}
rendering. The short-circuit is disabled for the whole table; for
every row the full format string interpolates, so \code{{n}} becomes
your formatter's output and \code{{p}} becomes the standard percentage.
For \code{n = 0}, that's \code{"0 (0.0\%)"}.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Force "0 (0.0\%)" for n = 0 rows by attaching a custom n formatter.
# The body of fmt$n can be the default integer rendering — its
# presence alone is what disables the zero-suppression branch.
pivot_across(
  saf_demo_card,
  statistic = list(
    continuous  = "\{mean\} (\{sd\})",
    categorical = "\{n\} (\{p\}\%)"
  ),
  fmt = list(n = function(x) sprintf("\%d", as.integer(x)))
)
}\if{html}{\out{</div>}}
}

\subsection{Pharma rounding (always-on default)}{

A percentage that would otherwise round to \code{0} (when the value
is positive but smaller than the chosen precision) renders as
\verb{<0.1}; one that would round to \code{100} (positive but smaller than
100) renders as \verb{>99.9}. The threshold is precision-aware:
\code{decimals = c(p = 2)} produces \verb{<0.01} / \verb{>99.99}. This matches
the pharma convention of never claiming exactly \verb{0\%} or \verb{100\%}
when at least one subject contributed.

Override per-stat via \code{fmt}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Show exact rounded percentages even at the extremes
pivot_across(
  data,
  statistic = "\{n\} (\{p\}\%)",
  decimals  = c(p = 1),
  fmt = list(p = function(x) sprintf("\%.1f", x * 100))
)
}\if{html}{\out{</div>}}

Your \code{fmt$p} receives the raw stat value (a proportion between
0 and 1) and returns the displayed string. The pharma-threshold
branch only fires inside the built-in \code{p} formatter and the
\code{decimals}-driven path, so any custom \code{fmt$p} bypasses it.
}
}
\examples{
# ---- Example 1: Demographics — long ARD to rendered spec ----
#
# Full pipeline from a `cards::ard_stack()`-style long ARD to a
# sorted `tabular_spec`. The multi-row continuous block (N /
# Mean (SD) / Median / Min, Max) sits above each categorical
# block; decimals are set per-stat (mean 1, sd 2, p 1) to match
# the CDISC convention.
n <- stats::setNames(saf_n$n, saf_n$arm_short)

saf_demo_card |>
  pivot_across(
    statistic = list(
      continuous = c(
        N           = "{N}",
        "Mean (SD)" = "{mean} ({sd})",
        Median      = "{median}",
        "Min, Max"  = "{min}, {max}"
      ),
      categorical = "{n} ({p}\%)"
    ),
    decimals = c(mean = 1, sd = 2, p = 1, median = 1, min = 0, max = 0),
    label    = c(AGE = "Age (years)", SEX = "Sex", RACE = "Race")
  ) |>
  tabular(
    titles = c(
      "Table 14.1.1",
      "Demographics and Baseline Characteristics",
      "Safety Population"
    ),
    footnotes = "Percentages based on N per treatment group."
  ) |>
  cols(
    variable   = col_spec(usage = "group", label = "Parameter"),
    stat_label = col_spec(label = "Statistic"),
    Placebo    = col_spec(
      label = sprintf("Placebo\nN=\%d", n["placebo"]),
      align = "decimal"
    ),
    `Xanomeline Low Dose` = col_spec(
      label = sprintf("Drug 50\nN=\%d", n["drug_50"]),
      align = "decimal"
    ),
    `Xanomeline High Dose` = col_spec(
      label = sprintf("Drug 100\nN=\%d", n["drug_100"]),
      align = "decimal"
    ),
    Total = col_spec(
      label = sprintf("Total\nN=\%d", n["Total"]),
      align = "decimal"
    )
  )

# ---- Example 2: Hierarchical SOC/PT AE table ----
#
# Hierarchical `cards::ard_stack_hierarchical()` output threaded
# through `pivot_across()`. The hierarchical ARD emits a
# (soc, label, row_type) triple plus one stat row per (arm, SOC, PT);
# `pivot_across()` folds the arm dimension to columns and preserves
# the hierarchy markers. Derive `indent_level` from `row_type` so
# `col_spec(indent_by = "indent_level")` drives the SOC -> PT
# indent on the `label` column.
wide <- saf_aesocpt_card |>
  pivot_across(statistic = "{n} ({p}\%)")
wide$indent_level <- as.integer(wide$row_type == "pt")

tabular(
  wide,
  titles = c(
    "Table 14.3.1",
    "Adverse Events by System Organ Class and Preferred Term",
    "Safety Population"
  ),
  footnotes = c(
    "Subjects are counted once per SOC and once per PT.",
    "Percentages based on N per treatment group."
  )
) |>
  cols(
    label    = col_spec(label = "SOC / PT", indent_by = "indent_level"),
    soc      = col_spec(visible = FALSE),
    row_type = col_spec(visible = FALSE),
    Placebo  = col_spec(
      label = sprintf("Placebo\nN=\%d", n["placebo"]),
      align = "decimal"
    ),
    `Xanomeline Low Dose` = col_spec(
      label = sprintf("Drug 50\nN=\%d", n["drug_50"]),
      align = "decimal"
    ),
    `Xanomeline High Dose` = col_spec(
      label = sprintf("Drug 100\nN=\%d", n["drug_100"]),
      align = "decimal"
    )
  )

# ---- Example 3: Hierarchical ARD (SOC / PT) ----
#
# `saf_aesocpt_card` carries an `ard_stack_hierarchical` shape with
# two grouping variables (AEBODSYS / AEDECOD). `pivot_across()`
# recognises the hierarchical structure and emits dedicated `soc`,
# `label`, and `row_type` columns so the SOC -> PT nesting survives
# the pivot. The result is ready for `tabular()` plus `sort_rows()`.
head(saf_aesocpt_card, 3)

wide <- saf_aesocpt_card |>
  pivot_across(statistic = "{n} ({p}\%)")
head(wide, 3)

# ---- Example 4: Multi-row continuous spec + label re-labelling ----
#
# `statistic = c(<label> = <template>, ...)` produces one display
# row per named entry — the canonical "N / Mean (SD) / Median /
# Min, Max" block for continuous variables. `label = c(...)`
# renames the variable headings emitted into the wide output.
saf_demo_card |>
  pivot_across(
    statistic = list(
      continuous = c(
        N            = "{N}",
        "Mean (SD)"  = "{mean} ({sd})",
        Median       = "{median}",
        "Q1, Q3"     = "{p25}, {p75}",
        "Min, Max"   = "{min}, {max}"
      ),
      categorical = "{n} ({p}\%)"
    ),
    label = c(
      AGE    = "Age (years)",
      WEIGHT = "Weight (kg)",
      HEIGHT = "Height (cm)",
      BMI    = "BMI (kg/m^2)"
    )
  )

}
\seealso{
\strong{Pipeline entry consumer:} \code{\link[=tabular]{tabular()}} — wraps the wide data
frame this helper returns.

\strong{Downstream spec-build verbs:} \code{\link[=cols]{cols()}} / \code{\link[=col_spec]{col_spec()}},
\code{\link[=headers]{headers()}}, \code{\link[=sort_rows]{sort_rows()}}, \code{\link[=style]{style()}},
\code{\link[=paginate]{paginate()}}, \code{\link[=preset]{preset()}}.

\strong{Terminal verbs:} \code{\link[=emit]{emit()}}, \code{\link[=as_grid]{as_grid()}}.
}
