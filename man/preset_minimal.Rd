% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preset_minimal.R
\name{preset_minimal}
\alias{preset_minimal}
\title{Minimal theme: one header rule, normal weight throughout}
\usage{
preset_minimal(.spec, ...)
}
\arguments{
\item{.spec}{\emph{The \code{tabular_spec} to apply the minimal theme to.}
\verb{<tabular_spec>: required}. Dot-prefixed so partial matching cannot
bind a \code{...} knob to the spec slot.}

\item{...}{\emph{Named preset knobs.} Forwarded verbatim to \code{\link[=preset]{preset()}}
(e.g. \code{font_size}, \code{font_family}, \code{orientation}, \code{paper_size},
\code{margins}), so a single call sets both the minimal look and the
page geometry.

\strong{Restriction:} the \code{rules} (and legacy \code{borders}) knob is owned by
this helper and may not be passed here; call \code{\link[=preset]{preset()}} directly
for a custom rule set.}
}
\value{
\emph{The updated \code{tabular_spec}.} Continue chaining with
\code{\link[=paginate]{paginate()}} / \code{\link[=style]{style()}}, then render via \code{\link[=emit]{emit()}} (or
resolve without I/O via \code{\link[=as_grid]{as_grid()}}).
}
\description{
Apply the stripped-down table look in one verb. The column-label
divider (\code{midrule}) becomes the only rule drawn, and every
bold-by-default surface renders in normal weight: the title block,
the column-header band, the subgroup banner, and the section-header
rows synthesized for \code{usage = "group"} columns. The analogue of
ggplot2's \code{theme_minimal()}, composable on the pipe between the build
verbs and the terminal \code{\link[=emit]{emit()}} / \code{\link[=as_grid]{as_grid()}}.
}
\details{
\strong{What it sets}, both at theme (lowest) precedence so an explicit
later \code{\link[=style]{style()}} wins:
\enumerate{
\item \strong{Rules.} Drops the booktabs \code{toprule} and \code{bottomrule} (the
outer frame), keeping the \code{midrule} under the column labels and the
muted column-spanner \code{spanrule}. Equivalent to \code{preset(rules = list(toprule = "none", bottomrule = "none"))}.
\item \strong{Weight.} Sets \code{bold = FALSE} on the title, column-header,
subgroup-label, and group-header surfaces. The HTML backend
overrides its \code{font-weight: 600} class default with an inline
\code{font-weight: normal}; the paginated backends (RTF / LaTeX / PDF /
DOCX) suppress the surface's bold run.
}

\strong{Last verb wins.} Because the weight layers ride the theme tier, a
later explicit \code{style(bold = TRUE, .at = cells_title())} (or any
surface) re-bolds it. Treat \code{preset_minimal()} as the theme baseline
and override individual surfaces afterwards.

\strong{Markdown.} GFM cannot represent colour / background / font on a
surface; rendering a styled surface to \code{.md} emits a one-time
\code{tabular_warning_fidelity} and degrades gracefully. Weight (bold) and
italic carry through.
}
\examples{
# ---- Example 1: Minimal AE overall summary ----
#
# The overall adverse-event summary with a single rule under the
# column labels and no bold anywhere. `preset_minimal()` is the theme
# baseline; the page stays at the session default geometry.
demo_n <- stats::setNames(cdisc_saf_n$n, cdisc_saf_n$arm_short)

tabular(
  cdisc_saf_ae,
  titles = c(
    "Table 14.3.1",
    "Overall Summary of Adverse Events",
    "Safety Population"
  ),
  footnotes = "Subjects counted once per category."
) |>
  cols(
    stat_label = col_spec(label = "Category"),
    placebo    = col_spec(label = "Placebo\nN={demo_n['placebo']}"),
    drug_50    = col_spec(label = "Drug 50\nN={demo_n['drug_50']}"),
    drug_100   = col_spec(label = "Drug 100\nN={demo_n['drug_100']}"),
    Total      = col_spec(label = "Total\nN={demo_n['Total']}")
  ) |>
  preset_minimal()

# ---- Example 2: Flat AE table under the minimal theme, then tint ----
#
# AE by SOC / PT rendered flat: the SOC subtotal row and its PT rows
# share the `label` stub, indented by `indent_level` (SOC at depth 0,
# PT at depth 1), so each SOC name appears once. `preset_minimal()`
# strips the outer frame and leaves the column-header band plain; a
# trailing `style()` tints just that band, showing a per-table
# override composing on top of the theme, and `font_size` forwards
# through `...`.

tabular(
  cdisc_saf_aesocpt,
  titles = c("Table 14.3.2", "Adverse Events by SOC and Preferred Term"),
  footnotes = "Subjects counted once per SOC and once per PT."
) |>
  cols(
    label    = col_spec(label = "SOC / PT", indent = "indent_level"),
    soc      = col_spec(visible = FALSE),
    row_type = col_spec(visible = FALSE),
    soc_n    = col_spec(visible = FALSE),
    n_total  = col_spec(visible = FALSE),
    placebo  = col_spec(label = "Placebo\nN={demo_n['placebo']}"),
    drug_50  = col_spec(label = "Drug 50\nN={demo_n['drug_50']}"),
    drug_100 = col_spec(label = "Drug 100\nN={demo_n['drug_100']}"),
    Total    = col_spec(label = "Total\nN={demo_n['Total']}")
  ) |>
  preset_minimal(font_size = 8) |>
  style(background = "#DBE4F0", .at = cells_headers())

}
\seealso{
\strong{Underlying verbs:} \code{\link[=preset]{preset()}} (the rule presets \code{"booktabs"} /
\code{"grid"} / \code{"frame"} / \code{"none"} live there as \code{rules} string sugar),
\code{\link[=style]{style()}}.

\strong{Target the surfaces it touches:} \code{\link[=cells_title]{cells_title()}},
\code{\link[=cells_headers]{cells_headers()}}, \code{\link[=cells_subgroup_labels]{cells_subgroup_labels()}},
\code{\link[=cells_group_headers]{cells_group_headers()}}.

\strong{Entry / terminal verbs:} \code{\link[=tabular]{tabular()}}, \code{\link[=emit]{emit()}}, \code{\link[=as_grid]{as_grid()}}.
}
