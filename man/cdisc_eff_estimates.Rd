% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{cdisc_eff_estimates}
\alias{cdisc_eff_estimates}
\title{Treatment-effect estimates by model}
\format{
A data frame with 4 rows and 5 columns:
\describe{
\item{\code{model}}{Model name (\code{"ANCOVA"}, \code{"MMRM"}, \code{"Cox PH"},
\code{"Bootstrap (1000 reps)"}).}
\item{\code{estimate}}{Numeric point estimate.}
\item{\code{lower_ci}, \code{upper_ci}}{Numeric 95\% CI bounds. The MMRM
row has \code{NA} bounds.}
\item{\code{p_value}}{Nominal p-value (numeric).}
}
}
\source{
Synthetic estimates following the
\verb{_archive/.../arframe-examples/tables/tte-summary.qmd} and
\code{efficacy-bor.qmd} shapes. Not derived from any patient-level
data — illustrative values only.
}
\usage{
cdisc_eff_estimates
}
\description{
Four competing efficacy models with their treatment-effect point
estimate, 95\% confidence-interval bounds, and nominal p-value.
Shaped as a numeric-cell table (one row per model) rather than the
usual pre-formatted character cells, so it exercises the
\code{col_spec(format = ...)} + \code{col_spec(na_text = ...)} cascade. One
row (\code{MMRM}) carries \code{NA} CI bounds to demonstrate \code{na_text}.
}
\examples{
# Numeric-cell efficacy table — format = "\%.2f" pins precision,
# na_text = "--" renders the MMRM row's NA bounds as dashes.
tabular(cdisc_eff_estimates, titles = "Treatment-effect estimates by model") |>
  cols(
    model    = col_spec(label = "Model", valign = "top"),
    estimate = col_spec(label = "Estimate", align = "decimal",
                        format = "\%.2f"),
    lower_ci = col_spec(label = "Lower\n95\% CI", align = "decimal",
                        format = "\%.2f", na_text = "--"),
    upper_ci = col_spec(label = "Upper\n95\% CI", align = "decimal",
                        format = "\%.2f", na_text = "--"),
    p_value  = col_spec(label = "p-value",  align = "decimal",
                        format = "\%.4f")
  )
}
\seealso{
\code{\link[=col_spec]{col_spec()}} for the formatting cascade these values
exercise.
}
\keyword{datasets}
