% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-demo.R
\name{demo-data}
\alias{demo-data}
\title{Demo datasets bundled with tabular}
\source{
Pre-summarised, illustrative values built by
\code{data-raw/bundle-demo.R} from \code{pharmaverseadam::adsl} /
\verb{::adae} / \verb{::advs} / \verb{::adrs_onco}. See the individual dataset
topics for the specific filters and summary statistics applied.
}
\description{
Five small, pre-summarised wide-format datasets used as the canonical
inputs for every roxygen \verb{@examples} block, vignette, and test in
\code{tabular}. The values are derived from \code{pharmaverseadam} (synthetic
CDISC Pilot 03 data) via \code{data-raw/bundle-demo.R}; they are not real
patient data. Each dataset is shaped to exercise a different cluster
of verbs.
}
\details{
\subsection{Lazy-loaded datasets}{

Available directly after \code{library(tabular)}:\tabular{lll}{
   Object \tab What it represents \tab Verbs exercised \cr
   \link{saf_demo} \tab Demographics, Safety Pop \tab \link{tb_table}, \link{tb_cols}, \link{tb_rows} (95\% case) \cr
   \link{saf_aeoverall} \tab High-level AE flags \tab \link{tb_table} minimal first-look \cr
   \link{saf_aesocpt} \tab AEs by SOC and PT \tab \link{tb_spans} + \link{tb_rows} page_by/indent_by \cr
   \link{saf_vital} \tab Vital signs at 2 visits \tab decimal alignment + multi-key group_by \cr
   \link{eff_resp} \tab BOR + derived ORR/DCR \tab derived rows, response taxonomies \cr
}

}

\subsection{Shared conventions}{

All five share these column / attribute conventions so the demo
programs in \verb{@examples} blocks read uniformly:
\itemize{
\item Arm columns named \code{placebo}, \code{drug_50}, \code{drug_100}; \code{Total} where
applicable.
\item \code{attr(x, "n")} is a named integer vector of BigN per arm
(e.g. \code{c(placebo = 86L, drug_50 = 84L, drug_100 = 89L, Total = 259L)}).
}
}
}
\examples{
# All five datasets are available immediately after library(tabular)
head(saf_demo)
attr(saf_demo, "n")

# Each is shaped for a specific verb scenario
nrow(saf_aeoverall)   # ~8 rows -- simple
nrow(saf_aesocpt)     # ~20 rows -- nested SOC/PT

}
\seealso{
\link{saf_demo}, \link{saf_aeoverall}, \link{saf_aesocpt}, \link{saf_vital},
\link{eff_resp}

Other demo-data: 
\code{\link{eff_resp}},
\code{\link{saf_aeoverall}},
\code{\link{saf_aesocpt}},
\code{\link{saf_demo}},
\code{\link{saf_vital}}
}
\concept{demo-data}
