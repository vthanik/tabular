% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cols.R
\name{cols_apply}
\alias{cols_apply}
\title{Apply one column spec to many columns}
\usage{
cols_apply(.spec, .cols, .col_spec)
}
\arguments{
\item{.spec}{\emph{The \code{tabular_spec} to extend.}
\verb{<tabular_spec>: required}. Dot-prefixed so partial matching
cannot bind a user name in another slot.}

\item{.cols}{\emph{Columns to match.}
\verb{<character | function>: required}. Either a character vector of
input column names in \code{.spec@data}, or a predicate
\verb{function(names) -> logical} evaluated against \code{names(.spec@data)}
(one logical per column, same length).

\strong{Restriction:} Named columns must exist in \code{.spec@data}. A
predicate must return a logical vector the length of
\code{names(.spec@data)}.
\strong{Tip:} No tidyselect helpers ship; pass a base vector
(\code{grep("^ARM", names(df), value = TRUE)}) or a predicate
(\verb{\\(nm) startsWith(nm, "ARM")}).}

\item{.col_spec}{\emph{The spec to field-merge onto every match.}
\verb{<col_spec>: required}. Built with \code{\link[=col_spec]{col_spec()}}.}
}
\value{
\emph{The updated \code{tabular_spec}.} Continue chaining with
\code{\link[=headers]{headers()}}, \code{\link[=sort_rows]{sort_rows()}}, \code{\link[=style]{style()}}.
}
\description{
Field-merge a single \code{\link[=col_spec]{col_spec()}} onto every column matched by
name or by a predicate. The vectorized companion to \code{\link[=cols]{cols()}} for
the common case of a variable number of treatment-arm columns that
all share the same display rule (decimal alignment, a numeric
format), so you avoid \code{do.call()} / \verb{!!!} splicing one named
argument per arm.
}
\details{
\strong{Field-merge, not replace.} \code{cols_apply()} reuses the same
field-by-field merge as repeated \code{\link[=cols]{cols()}} calls: a non-default
field on \code{.col_spec} overrides; a default-valued field leaves any
prior attribute on the matched column intact. Set the shared rule
across arms first, then refine an individual arm with a later
\code{\link[=cols]{cols()}} call (or the reverse).
}
\examples{
# ---- Example 1: Decimal-align every arm column by name vector ----
#
# Demographics table whose treatment-arm columns are selected by a
# name vector (`grep()` against the data) and given one shared
# decimal-alignment spec, while the two row-label columns keep
# their own roles set with `cols()`.
arm_cols <- grep("^placebo$|^drug_|^Total$", names(saf_demo), value = TRUE)

tabular(
  saf_demo,
  titles = c(
    "Table 14.1.1",
    "Demographics and Baseline Characteristics",
    "Safety Population"
  )
) |>
  cols(
    variable   = col_spec(usage = "group", label = "Parameter"),
    stat_label = col_spec(label = "Statistic")
  ) |>
  cols_apply(arm_cols, col_spec(align = "decimal")) |>
  sort_rows(by = c("variable", "stat_label"))

# ---- Example 2: Select arm columns with a predicate ----
#
# Best Overall Response table. The arm columns are matched with a
# predicate over the column names; the hidden sort helpers and the
# response label are declared with `cols()`. The predicate scales
# to any number of arms without editing the call.
bor_levels <- c(
  "CR", "PR", "SD", "NON-CR/NON-PD", "PD", "NE", "MISSING",
  "ORR (CR + PR)", "CBR (CR + PR + SD)",
  "DCR (CR + PR + SD + NON-CR/NON-PD)", "95\% CI (Clopper-Pearson)"
)
eff <- eff_resp
eff$stat_label <- factor(eff$stat_label, levels = bor_levels)

tabular(
  eff,
  titles = c(
    "Table 14.2.1",
    "Best Overall Response and Response Rates",
    "Efficacy Evaluable Population"
  )
) |>
  cols(
    stat_label  = col_spec(label = "Response"),
    row_type    = col_spec(visible = FALSE),
    groupid     = col_spec(visible = FALSE),
    group_label = col_spec(visible = FALSE)
  ) |>
  cols_apply(
    \(nm) nm \%in\% c("placebo", "drug_50", "drug_100"),
    col_spec(align = "decimal")
  ) |>
  sort_rows(by = c("groupid", "stat_label"))

}
\seealso{
\strong{Companion verbs:} \code{\link[=cols]{cols()}} attaches per-column specs by name;
\code{\link[=col_spec]{col_spec()}} builds the spec.

\strong{Sibling build verbs:} \code{\link[=headers]{headers()}}, \code{\link[=sort_rows]{sort_rows()}},
\code{\link[=style]{style()}}, \code{\link[=paginate]{paginate()}}, \code{\link[=preset]{preset()}}.
}
