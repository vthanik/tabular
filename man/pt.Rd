% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/units.R
\name{pt}
\alias{pt}
\alias{px}
\alias{pct}
\alias{is_unit}
\title{Typed unit helpers}
\usage{
pt(x)

px(x)

pct(x)

is_unit(x)
}
\arguments{
\item{x}{\emph{Magnitude.} \verb{<numeric(1)>: required}. Non-negative,
finite. For \code{pct()}, a percentage in \verb{[0, 100]}.}
}
\value{
\emph{A \code{tabular_unit} object} — a length-2 list \code{list(value, unit)} with class \code{"tabular_unit"}.
}
\description{
Tag a width or size with its unit so a style argument is
unambiguous about points versus pixels versus percent. Accepted
anywhere \code{tabular} takes a width or size; a bare numeric is still
interpreted as points. \code{pct()} expresses a proportional width
(e.g. a column at 50\% of the table width).
}
\examples{
# ---- Example 1: Disambiguate a rule width ----
#
# A 0.75-point hairline is unambiguous as pt(0.75); a bare 0.75
# also means points, but the typed form documents intent.
pt(0.75)
px(1)
pct(50)

}
\seealso{
\code{\link[=brdr]{brdr()}} for border specifications that accept these.
}
