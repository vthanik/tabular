% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tb_cols.R
\name{tb_cols}
\alias{tb_cols}
\title{Column setup}
\usage{
tb_cols(
  spec,
  labels = NULL,
  width = NULL,
  align = NULL,
  visible = NULL,
  n = NULL
)
}
\arguments{
\item{spec}{A \code{tabular_spec} from \code{\link[=tb_table]{tb_table()}} or another verb.}

\item{labels}{Named \code{character()}. Display labels for the column
header. Names are columns in \code{spec@data}; values are the strings
that appear in the rendered header row. \code{NULL} (default) leaves
labels unchanged.}

\item{width}{Named \code{numeric()} of widths in inches. \code{NULL} (default)
defers to the backend's auto-fit policy.}

\item{align}{Named \code{character()}. Values must be one of \code{"left"},
\code{"center"}, \code{"right"}, \code{"decimal"}. The wildcard name \code{"*"} sets
the default alignment for all columns not named explicitly.
\code{NULL} (default) leaves alignment unchanged.}

\item{visible}{Named \code{logical()}. \code{FALSE} hides a column from the
rendered output (the column stays available for grouping and
sorting via \code{\link[=tb_rows]{tb_rows()}}). \code{NULL} (default) leaves visibility
unchanged.}

\item{n}{Named \code{integer()} of BigN counts (one per treatment arm,
optionally \code{Total}). Backends typically append these as
\code{"(N=xx)"} in the column header. \code{NULL} (default) leaves BigN
unchanged.}
}
\value{
The updated \code{tabular_spec}.
}
\description{
Configure column labels, widths, alignment, visibility, and BigN.
All arguments are flat named vectors keyed by the column names in
\code{spec@data}. Each call \strong{merges} into the existing column
configuration: fields you supply are updated; fields you omit stay
as they were.
}
\section{Wildcard}{

Only \code{align} accepts the wildcard name \code{"*"}. Other arguments
reject \code{"*"} -- every name in \code{labels} / \code{width} / \code{visible} / \code{n}
must match a column in \code{spec@data}.
}

\section{Errors}{

Raises \code{tabular_error_input} when:
\itemize{
\item \code{spec} is not a \code{tabular_spec}.
\item Any argument is unnamed, has \code{NA} names, or has duplicate names.
\item Any name (other than \code{"*"} in \code{align}) is not a column in
\code{spec@data}.
\item \code{labels} is non-character, \code{width} is not positive finite,
\code{align} value is outside the allowed set, \code{visible} is
non-logical, or \code{n} is not a positive whole number.
}
}

\examples{
# 95\% case -- label + alignment + BigN
saf_demo |>
  tb_table(titles = "Demographics") |>
  tb_cols(
    labels = c(placebo = "Placebo", drug_50 = "Drug 50mg"),
    align  = c("*" = "decimal"),
    n      = c(placebo = 86L, drug_50 = 84L)
  )

# Hide a helper column used only for grouping
saf_aesocpt |>
  tb_table(titles = "AEs by SOC and PT") |>
  tb_cols(visible = c(soc = FALSE))
}
\seealso{
\code{\link[=tb_table]{tb_table()}}, \code{\link[=tb_rows]{tb_rows()}}, \code{\link[=tb_render]{tb_render()}}.

Other structure: 
\code{\link{tb_rows}()},
\code{\link{tb_spans}()}
}
\concept{structure}
