% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col_spec.R
\name{col_spec}
\alias{col_spec}
\title{Per-column display specification}
\usage{
col_spec(
  usage = NULL,
  label = NA_character_,
  format = NULL,
  visible = TRUE,
  width = NA_real_,
  align = NULL,
  na_text = ""
)
}
\arguments{
\item{usage}{One of \code{"display"}, \code{"group"}, \code{"across"},
\code{"computed"}, or \code{NULL} (auto-default in \code{cols()}).
Accepts:
\itemize{
\item \strong{\code{"display"}} — pass-through column; rendered as-is.
\item \strong{\code{"group"}} — row-label column; repeat-suppression and
continuation-page repeat keys to this column.
\item \strong{\code{"across"}} — column whose unique values become new
output columns via \code{pivot_across()}.
\item \strong{\code{"computed"}} — derived column produced by a
\code{derive_spec}.
\item \strong{\code{NULL}} (default) — inferred in \code{cols()} (always
\code{"display"}).
}}

\item{label}{Display label for the column header. Single string;
embed \verb{\\n} for line breaks. Embed BigN inline via
\code{paste()} / \code{sprintf()} (no dedicated BigN field).
\code{NA_character_} (default) means use the input column name.}

\item{format}{Post-cell formatter applied at \code{engine_format}:
\itemize{
\item \strong{character} — a \code{sprintf()} template (e.g. \code{"\%.1f"}).
\item \strong{function} — a unary function taking one column of values
and returning a character vector of the same length.
\item \strong{\code{NULL}} (default) — backend-default formatting.
}}

\item{visible}{Logical. \code{FALSE} hides the column from output;
useful for keeping a column for sort / derive / pagination
without rendering it. Default \code{TRUE}.}

\item{width}{Column width in inches. \code{NA_real_} (default) leaves
widths to backend auto-fit. Must be positive and finite when set.}

\item{align}{One of \code{"left"}, \code{"center"}, \code{"right"}, \code{"decimal"},
or \code{NULL} (backend default).
\code{"decimal"} aligns numeric content on the decimal mark via the
active preset's \code{decimal_metrics}.}

\item{na_text}{Single string substituted for \code{NA} cells before the
\code{format} step. Default \code{""}.}
}
\value{
A \code{col_spec} S7 object.
}
\description{
Build a column specification used inside \code{cols()}. Each col_spec
carries the seven display attributes for one column of the input
data frame; \code{cols()} then attaches it to a \code{tabular_spec} keyed by
the input column name.
}
\examples{
# Defaults: pass-through display column
col_spec()

# Group (row-label) column with an explicit display label
col_spec(usage = "group", label = "Parameter")

# BigN embedded inline — join from `saf_n` so the denominator
# comes from a single source of truth.
n <- setNames(saf_n$n, saf_n$arm_short)
col_spec(
  label = sprintf("Drug 50\nN=\%d", n["drug_50"]),
  align = "decimal"
)

# Hidden column kept for sorting / pagination only
col_spec(visible = FALSE)

# sprintf formatter for one-decimal display
col_spec(format = "\%.1f")

}
