% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figure.R
\name{figure}
\alias{figure}
\title{Wrap a plot or image in submission chrome}
\usage{
figure(
  plot,
  titles = NULL,
  footnotes = NULL,
  width = NULL,
  height = NULL,
  halign = "center",
  valign = "middle",
  dpi = 300,
  meta = NULL
)
}
\arguments{
\item{plot}{\emph{The figure to display.} One of: a \code{ggplot} object; a
recorded base plot from \code{\link[grDevices:recordPlot]{grDevices::recordPlot()}}; a
zero-argument function that draws to the active device when called;
a length-1 path to a \code{.png}, \code{.jpg}, or \code{.jpeg} file; or a \code{list}
of any of these for a multi-page figure.

\strong{Tip:} a list may mix kinds freely — a ggplot, a recorded plot,
and a PNG path can share one multi-page figure.}

\item{titles}{\emph{Title lines above the figure.} \verb{<character> | NULL}.
One element per line, up to four; same \code{{glue}} interpolation and
\code{\link[=md]{md()}} / \code{\link[=html]{html()}} inline formatting as \code{\link[=tabular]{tabular()}}. \code{NULL}
draws no titles.}

\item{footnotes}{\emph{Footnote lines below the figure.} \verb{<character> | NULL}. One element per line; same interpolation and inline
formatting as \code{titles}. \code{NULL} draws no footnotes.}

\item{width}{\emph{Drawn image width in inches.} \verb{<numeric(1)> | NULL}.
\code{NULL} fills the full printable width.}

\item{height}{\emph{Drawn image height in inches.} \verb{<numeric(1)> | NULL}.
\code{NULL} uses 70\% of the printable height, leaving the image centred
in the body region.}

\item{halign}{\emph{Horizontal placement in the content box.}
\verb{<character(1)>}. One of:
\itemize{
\item \code{"left"}
\item \code{"center"} (default)
\item \code{"right"}
}}

\item{valign}{\emph{Vertical placement in the content box.}
\verb{<character(1)>}. One of:
\itemize{
\item \code{"top"}
\item \code{"middle"} (default)
\item \code{"bottom"}
}

\strong{Note:} continuous backends (HTML / Markdown) render the figure
contained to the viewport with no fixed page height, so \code{valign} is a
no-op there; the paged backends honour it exactly.}

\item{dpi}{\emph{Raster resolution for plot inputs.} \verb{<numeric(1)>}.
Resolution in dots per inch for PNG rasterisation. Ignored for file
inputs (passed through unchanged) and for vector PDF targets.}

\item{meta}{\emph{Per-page token data frame.} \verb{<data.frame> | NULL}.
Multi-page only: one row per plot, whose columns become \code{{token}}
values in that page's \code{titles} / \code{footnotes}. Ignored (with a
warning) for a single-plot figure.}
}
\value{
\emph{A \code{figure_spec}.} Pass it to \code{\link[=emit]{emit()}} to write a file, or
print it to preview the figure inline.
}
\description{
Builds a figure display, the "F" in TFL. \code{figure()} takes a ggplot, a
recorded base-R plot, a zero-argument drawing function, or a path to a
PNG / JPG file, and surrounds it with the same canonical submission
chrome as a table: up to four centred title lines, footnotes, and the
per-page header / footer drawn from the active \code{\link[=preset]{preset()}}. Pass a
list to emit one figure per page in a single file.
}
\details{
\strong{Two-axis placement.} The drawn image is usually smaller than the
body content box (the default \code{height} is 70\% of the printable
height), so both \code{halign} and \code{valign} are load-bearing on the paged
backends. They place the image in the content box independently —
horizontally (\code{left} / \code{center} / \code{right}) and vertically
(\code{top} / \code{middle} / \code{bottom}), defaulting to centred on both axes.
Paged backends (RTF / PDF / DOCX) honour \code{valign} exactly against the
content-box height. The continuous backends (HTML / Markdown) render the
figure responsively, contained to the viewport, so \code{halign} still
applies but \code{valign} is a no-op there.

\strong{Format-aware rasterisation.} Plot inputs render to vector PDF for
\code{.pdf} / \code{.tex} targets and to PNG at \code{dpi} for every other backend;
file inputs pass through byte-for-byte. No raster work happens until
\code{\link[=emit]{emit()}}.
}
\examples{
# ---- Example 1: a single base-R figure with submission chrome ----
#
# A zero-argument drawing function is the simplest portable input: it
# draws to whatever device the backend opens. Here, subjects enrolled
# per treatment arm from the bundled BigN frame, wrapped in the
# canonical title block and a population footnote.
arms <- cdisc_saf_n[cdisc_saf_n$arm_short != "Total", ]

draw_enrollment <- function() {
  barplot(
    arms$n,
    names.arg = arms$arm_short,
    ylab = "Subjects enrolled",
    col = "grey70"
  )
}

fig <- figure(
  draw_enrollment,
  titles = c(
    "Figure 14.1.1",
    "Subjects Enrolled by Treatment Arm",
    "Safety Population"
  ),
  footnotes = "Total enrolled: N = 254."
)
fig

# ---- Example 2: one figure per page from a list ----
#
# A list input emits one figure per page in a single file. Each arm
# gets its own page; the kinds may mix (a ggplot, a recorded plot, or
# a PNG path could share the list). Bottom-anchored here to show the
# two-axis placement.
draw_arm <- function(i) {
  force(i)
  function() {
    barplot(arms$n[i], names.arg = arms$arm_short[i], col = "grey70")
  }
}

per_arm <- figure(
  lapply(seq_len(nrow(arms)), draw_arm),
  titles = c("Figure 14.1.2", "Enrollment by Arm, One Page per Arm"),
  valign = "bottom"
)
per_arm

}
\seealso{
\strong{Terminal verb:} \code{\link[=emit]{emit()}} (write the figure to a file).

\strong{Shared chrome:} \code{\link[=preset]{preset()}} / \code{\link[=set_preset]{set_preset()}} (page geometry,
fonts, header / footer), \code{\link[=tabular]{tabular()}} (the table sibling).

\strong{Class predicate:} \code{\link[=is_figure_spec]{is_figure_spec()}}.
}
