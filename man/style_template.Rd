% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style_template.R
\name{style_template}
\alias{style_template}
\alias{is_style_template}
\title{Reusable style template (for house-style presets)}
\usage{
style_template()

is_style_template(x)
}
\arguments{
\item{x}{\emph{Any R object.} The predicate inspects the class via
\code{\link[=inherits]{inherits()}}; no other introspection is performed.}
}
\value{
\emph{A \code{tabular_style_template}} — a small S3 list with a
\code{layers} slot. Pipe through \code{\link[=style]{style()}} to add layers.
}
\description{
Build a reusable, composable style template by chaining
\code{\link[=style]{style()}} calls against a \code{tabular_style_template}. The
template carries an ordered list of \code{\link{style_layer}} records and
can be attached to \code{\link[=preset]{preset()}} / \code{\link[=set_preset]{set_preset()}} as a \verb{style =}
argument — every downstream \code{\link[=tabular]{tabular()}} chain then inherits the
template's layers via the engine cascade.
}
\details{
\strong{One verb, two surfaces.} The same \code{style(.spec_or_template, ..., at = ...)} call that attaches a layer to a per-table spec
also accumulates layers onto a template. Symmetric API — no need
to learn a second function for the multi-table use case.

\strong{Submission workflow.} A submission typically renders 100–200
tables with one visual identity. Build the template once at the
top of the submission script, pass it to \code{set_preset(style = template)}, and every subsequent \code{tabular()} produces output that
inherits the same column-header rules, group-header bolding,
title spacing, and outer-frame borders without a single per-table
\code{style()} call.

\strong{Cascade order.} Engines apply layers low-to-high priority:
backend defaults → session preset's \verb{@style} → spec preset's
\verb{@style} → per-spec \code{style()} layers. Later layers override prior
ones per attribute; NA fields leave the prior layer's value in
place.
}
\examples{
# ---- Sponsor "house style" composed once ----
#
# The result becomes the default look for every table rendered
# against this preset. No per-table style() boilerplate.
house <- style_template() |>
  style(bold = TRUE, at = cells_headers(level = -1)) |>
  style(bold = TRUE, at = cells_group_headers()) |>
  style(
    border_top    = brdr("thick", "double"),
    border_bottom = brdr("thick", "double"),
    at = cells_headers()
  ) |>
  style(blank_above = 1, blank_below = 1, at = cells_title())

length(house$layers)

# ---- Verify class ----
is_style_template(house)

}
\seealso{
\strong{Style verb:} \code{\link[=style]{style()}} — the same verb chains onto a spec or
a template.

\strong{Locations:} \code{\link{cells_body}} — locations that name the \emph{where}
half of every layer.
}
