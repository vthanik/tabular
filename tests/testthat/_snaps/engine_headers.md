# engine_headers() error snapshot

    Code
      tabular:::engine_headers(spec)
    Condition
      Error:
      ! `headers()` band "Bad" spans non-contiguous columns.
      x 2 intruder columns between its leaves: "drug_100" and "drug_50".
      i Reorder data columns upstream, or place the intruder under the same band.

